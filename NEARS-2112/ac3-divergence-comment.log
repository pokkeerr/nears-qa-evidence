AC3 — the fix's own comment above captureCuisineTaxonomySnapshot() states it is
NOT a copy of NEARS-1834's rename-based approach, with the one-line reason.

Admin/tests/Feature/CuisineTaxonomySchemaTest.php:51-55

    /**
     * NEARS-2112: row-data snapshot/restore around the REAL down()/up() calls below --
     * not NEARS-1834's rename-and-purge -- because this test's Schema::hasTable()
     * assertions need the table to actually flip, which a rename would falsify.
     */
    private function captureCuisineTaxonomySnapshot(): void

RESULT: PASS — explicitly names NEARS-1834, states divergence ("not ...
rename-and-purge"), and gives the one-line reason (Schema::hasTable()
assertions need the table to actually flip).
