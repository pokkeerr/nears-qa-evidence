# NEARS-3668 QA progress

Device: emulator-5556 (fresh install com.izzes.nears.nears_nears_3668_fcm_guard), no google-services.json present (forced-failure condition).
Backend: own php artisan serve :8011 from this worktree.

## Run 1 (manual "Use my current location" -> "Pick Location" path, ex-NEARS-3671 entry point)
- AC1: PASS. Tapped "Pick Location" -> logcat shows exactly one [FAIL] type=FcmTopicConfigFailure line at 16:55:37.891, immediately followed by "home load started" / navigation to home. No stall.
- AC2: PASS. `adb logcat -d | grep -c FcmTopicConfigFailure` = 1 (exact single line, matches msg="configureFirebaseMessaging: FCM topic subscribe/unsubscribe failed").
- Home landed fully (after backend dev-server hiccup unrelated to fix - php artisan serve single-thread choked on the burst of parallel home API calls, restarted, Retry -> full home render with stores). This is a QA env limitation (documented as regression_bugs/followup), not a code defect in this fix.

## Run 2 (second independent fresh install, uninstall+reinstall on same device emulator-5556, per AC3)
- AC1: PASS. Fresh install, no google-services.json, granted location permission, tapped Pick Location -> exactly one [FAIL] type=FcmTopicConfigFailure at 17:03:32.953, immediately followed by "home load started" and navigation to home (login-suggestion sheet shown = home landed).
- AC2: PASS. Exact-pid-scoped grep (pid 18935) confirms exactly 1 occurrence.
- AC3: PASS (2/2 independent fresh installs both proceed to home without stalling).

## Regression item 3 (both ex-duplicate entry points)
- Manual pick-map commit path (ex-NEARS-3671): LIVE verified in both runs above -- `PickMapScreen._onPickAddressButtonPressed` -> `pickAddressFromMap` (PickMapAction.saveAddress) -> `LocationController.saveAddressAndNavigate` -> `_prepareZoneData` -> `autoNavigate` (line 926 call site) -> `_saveDataAndFirebaseConfig` -> `configureFirebaseMessaging`. Confirmed via logcat: "pick-map: saving location and navigating" immediately preceding the single FcmTopicConfigFailure line, both runs.
- Auto-navigate path (ex-NEARS-3562), `LocationController.autoNavigate` called directly from `navigateToLocationScreen` (line 1489, no pick-map screen involved) for a returning user with a saved address: attempted live via login (customer@nears.com) to reach a post-login navigateToLocationScreen('sign-in') call, but this was BLOCKED by an unrelated, pre-existing unguarded `FirebaseMessaging.instance.subscribeToTopic` call in `AuthRepository.updateToken()` (auth_repository.dart:256) -- NOT part of this ticket's diff (different file/method) -- which throws uncaught and stalls the post-login flow before reaching navigateToLocationScreen. Filed as a followup regression-candidate (see below), not fixed here (out of scope).
  - Static/structural confirmation instead: grep confirms `configureFirebaseMessaging` has exactly ONE call site in the entire codebase (location_controller.dart:1133), and `autoNavigate`'s only two branches (isDesktop-module-dialog vs. else) BOTH terminate in that same call to `_saveDataAndFirebaseConfig` (lines 966-998 read live). Since Run 1/2 already exercise `autoNavigate` itself (not just `_saveDataAndFirebaseConfig` directly) via the `_prepareZoneData` call site, and there is no second, alternate code path around the guard, the fix structurally covers the line-1489 call site identically -- but this specific alternate TRIGGER (returning-user auto-navigate without pick-map) was not independently live-driven end-to-end due to the blocking unrelated defect above. Flagging as a stated scope limitation, not silently skipped.

## AC4 (happy-path regression, Firebase-configured build)
- PASS. Used the documented "unsuffixed control boot" recipe (userapp-navigation-guide.md NEARS-3544 gotcha): copied primary tree's google-services.json into worktree, unset NEARS_PKG_SUFFIX, bypassed qa-run.sh, `flutter run -d emulator-5556` directly (unsuffixed com.izzes.nears, matches the file's one registered client). Firebase initialized for real (D/FirebaseSessions: "Fetching settings from server.", notification permission prompt shown, "[INFO] msg=\"FCM registration id obtained\""). Drove language->permission->pick-map->"Pick Location" identical to runs 1/2. Result: "pick-map: saving location and navigating" -> "home load started" -> home landed (login-suggestion sheet), with ZERO FcmTopicConfigFailure lines for this pid (confirmed via grep count=0). No new failure log introduced on the working case -- guard doesn't regress the happy path.
- Cleaned up per guide: uninstalled com.izzes.nears, removed the copied google-services.json from the worktree afterward.

## Regression sweep (item 5) + automated backstop (item 6)
- flutter test test/features/location/ (touched app, full location feature folder): 259/259 tests passed, including the dedicated NEARS-3668 guard test file. No new failures.
- Home load, module picker (zero-store empty state, zone 454), guest login-suggestion sheet, sign-in screen, profile screen all exercised live across the 3 boots above with no new red-screens/overflow beyond the ONE unrelated pre-existing defect noted below.

## Unrelated pre-existing defect found (NOT part of this ticket's diff) -- followups[]
`AuthRepository.updateToken()` (UserApp/lib/features/auth/domain/repositories/auth_repository.dart:256) calls `FirebaseMessaging.instance.subscribeToTopic(AppConstants.topic)` with NO try/catch (same defect class NEARS-3668 just fixed elsewhere, but a completely different file/method, untouched by this diff). On this Firebase-less worktree, logging in (customer@nears.com, tested via Sign In) produces an uncaught `[core/no-app]` exception (full stack trace captured, see evidence log) that propagates up through `AuthService.login -> AuthController.login -> AuthController.validateAndLogin -> _SignInViewState._login`, silently stalling the post-login navigation -- the user is left stuck on the sign-in form even though the login itself succeeded server-side (token was saved; backing out manually landed on the Profile tab showing the now-logged-in user). This blocked live verification of the `navigateToLocationScreen`'s direct `autoNavigate` call site (line 1489, the "returning user" trigger) within this QA session. Flagged as followups[] regression-candidate, not fixed here (out of scope/diff).
