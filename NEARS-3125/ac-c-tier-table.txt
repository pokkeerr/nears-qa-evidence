# AC(c) single tier table — cat new-task.md run-sprint.md | grep -c '| Tier | Who decides'
1
.claude/commands/new-task.md:252:| Tier | Who decides | Mechanism |
other 'Tier' mentions in run-sprint.md are pointers to new-task.md §Advisors / design §10:
70:explicit approval — the owner's, or advisor-product's for a Tier B plan (next). **Tier gate (NEARS-3125 — tiers per `new-tas
70:lan whose tickets all sit INSIDE the approved epic set is **Tier B** — the one consult the runner makes itself, because no p
70:r gate). A plan reaching OUTSIDE the approved epic set is **Tier C** — the owner only; no advisor decides it. Interactive ow
93:y there.) · a `NEEDS_DECISION` from any agent → technical = Tier A: the per-ticket conductor asks the domain advisor and con
93:p-2** (2 failed fix→re-QA cycles) → overriding a QA FAIL is Tier C → parked · **merge-sentinel BLOCKED** → a trivial resolut
93:Tier A (the conductor); merging past a semantic conflict is Tier C → parked · a **batch blocker ticket failed** → Tier C par
[7] code-reviewer ac_c_readthrough row: PASS (cited from conductor brief)
