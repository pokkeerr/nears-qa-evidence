# AC(d) profile Statuses line
14:- **Statuses (all exist):** Preparing → Ready → In Progress → In Review → In QA → Done, plus **Blocked** (Jira status name `BLOCKED`, status id 10044 — off the happy path: the Tier C park of `.claude/commands/new-task.md` §"Advisors", NEARS-3125). Advisor labels: `advisor-decided` (a Tier B decision posted for later owner ratification) · `waiting-on-owner` (paired with Blocked; the owner's reply on the ticket resumes the run). Discover transition ids with `getTransitionsForJiraIssue` per issue — never hardcode ids.

JQL half (run by conductor, cited — QA holds no JQL grant): project = NEARS AND status = Blocked resolved, 5 issues (NEARS-2816, 2791, 1904, 1711, 1217)
