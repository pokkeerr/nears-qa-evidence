# read-through spot-check
new-task.md:225 pilot-optional + tier-gate exception:
Pilot rule (design §12): every consult point is OPTIONAL
Exception — tier gates ([2b] here, [3] in `/run-sprint`) are fail-closed:** a timeout or absence there never proceeds; each gate states its own retry-then-owner fallback.
new-task.md:92 [2b]:
Tier gates are fail-closed:** if `advisor-product` is absent or times out here, do NOT proceed — retry once (`bash scripts/advisors/up.sh advisor-product` if absent, then re-ask), then fall back to an interactive owner ask;
run-sprint.md:93 [4]:
(The [3] plan-approval consult is the sole exception: it runs before any per-ticket conductor exists, so the runner asks directly there.)
workflow-profile.md:29 roster + reply dir:
roster `docs/workflow/advisors/roster.yaml`
reply dir `$HOME/.nears/advisors/replies/<qid>.md`
