# NEARS-3412 QA progress log

- AC1 (Admin POS add-to-cart error toast): PASS — forced 500 JSON (invalid product id -> `Attempt to read property "module" on null`, `[FAIL]` logged with trace_id/correlation_id), toast "Something went wrong", no raw-JSON nav. Evidence: ac1-admin-addtocart-error-toast.png
- AC2 (Admin POS place-order guard reject): PASS — missing delivery address -> 403 `{"errors":[{"code":"address","message":"Please select a valid delivery location on the map"}]}`, toast matches, page stayed on /admin/pos. Evidence: ac2-admin-placeorder-error-toast.png
  - NOTE: first pass through this AC was invalidated by a worktree environment defect (Admin/vendor was a SYMLINK to the primary tree, in violation of the documented hard rule) — see drift[] in envelope. Re-ran after `composer install` fixed the autoloader; all results below are from the corrected environment.
- AC3a (Vendor POS add-to-cart error toast): PASS — same invalid-id 500 repro, toast "Something went wrong". Evidence: ac3a-vendor-addtocart-error-toast.png
- AC3b (Vendor POS place-order guard reject): PASS — empty cart -> 403 `{"errors":[{"code":"cart","message":"Cart empty warning"}]}`. Evidence: ac3b-vendor-placeorder-error-toast.png
- AC4 (Admin edit-order-update guard reject + confirm dialog): PASS — entered edit mode, reduced order total below store minimum_order, SweetAlert "are you sure... submit all changes" confirmed VISIBLE and required before the AJAX call fired, then 403 `{"errors":[{"code":"minimum_order","message":"You need to order at least "}]}` (note: message missing amount — pre-existing lang-file placeholder bug, unrelated to this ticket, filed as regression_bugs). Evidence: ac4-admin-editorder-error-toast.png
- AC5 regression:
  - Admin add-to-cart happy path: PASS, toast "Product has been added in cart". ac5-admin-addtocart-success.png
  - Vendor add-to-cart happy path: PASS, same toast. ac5-vendor-addtocart-success.png
  - Vendor place-order happy path: PASS, toast "Order placed successfully", real order (id 100142) created in DB, reload landed back on POS. (implicit in ac3b probe's first run before it was redirected to the guard-reject repro)
  - Admin edit-order-submit happy path: PASS, toast "Order updated successfully", order 91220 updated_at bumped, page reloaded to same URL. ac5-editorder-success.png
  - Admin POS place-order happy path: NOT directly demonstrated live (delivery-address widget is a Google Maps picker, impractical to drive headlessly without a real Maps key/network) — covered by code-symmetry (identical `$.post().done()/.fail()` JS shape verified byte-for-byte against Vendor's, which WAS demonstrated live) + the AC2 guard-reject demo proving the same request plumbing (CSRF, ajax headers, serialize) is intact. Noted as a scoped limitation, not skipped silently.
- Security note (catch-all `\Exception` in both `place_order` methods, no `ajax()` branch): confirmed via code read (POSController.php:944 `catch (\Exception $e) { ... } Toastr::warning(...); return back()...` — no `$request->ajax()` check). Could not safely trigger a genuine mid-transaction exception via the UI without DB tampering (forbidden, read-only DB rule) — marked UNVERIFIABLE live, code-confirmed as a real (pre-disclosed) gap.
- Non-goal check (discount/tax/extra-charge AJAX, customer-create modal): verified via diff — zero touched lines near those blocks in either admin-views/pos/index.blade.php or vendor-views/pos/index.blade.php.
- Automated backstop: `vendor/bin/phpunit --filter "OrderDetailBuilder|WalletOrderPlaceLock|GroupOrderPlacement"` -> 27/27 passed, 122 assertions, 0 failures (1 PHPUnit deprecation notice, non-blocking).
