NEARS-1483 QA — AC2(b) dialog identity, asserted POSITIVELY by on-screen strings
device: emulator-5558 | entry path: wallet add-fund | build: 8f2eb548

LIVE screen (payment_screen.dart, the one actually routed) _exitApp() ->
  on-screen: 'Do you want to cancel this add fund?'  (key do_you_want_to_cancel_this_add_fund)
  on-screen: 'Cancel Add Fund'                        (key cancel_add_fund)
  => FundPaymentDialogWidget. CONFIRMED by identity, not by 'something popped'.
  PaymentFailedDialog strings ('Are you agree with this order fail?', 'Cancel Order',
  'Switch to Cash On Delivery') are ABSENT from the tree.

CHANGED file (payment_webview_screen.dart) _exitApp() gate:
  if ((addFundUrl == null || addFundUrl.isEmpty) || !pluginPaymentGateways) -> PaymentFailedDialog
  config/api/v1/config -> digital_payment_info.plugin_payment_gateways = false
  => !false == true => the OR short-circuits true on EVERY entry path,
     so the wallet path would open PaymentFailedDialog, not FundPaymentDialogWidget.
  Backend source: ConfigController.php:262 <- config('get_payment_publish_status'),
  which is supplied by the closed-source Gateways addon that is ABSENT by design.
