NEARS-692 AC1 — raw json_decode($zone_id) request-scope sites in ProductLogic.php
date: Wed Jul  1 13:39:09 +04 2026

# uncommented raw json_decode($zone_id) sites:
  NONE (0 sites) — PASS

# the only json_decode($zone_id) matches are the 2 commented-out legacy lines:
448:    //             })->whereIn('zone_id', json_decode($zone_id, true));
473:    //             })->whereIn('zone_id', json_decode($zone_id, true));

# parseZoneIds usages (should be 17):
17
