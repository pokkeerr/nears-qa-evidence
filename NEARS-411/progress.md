# NEARS-411 QA progress checkpoint (cycle 0)
Build: feat/NEARS-411-rewards-reskin @4407a64c | device emulator-5554 | user michael.brown@demo.com (user 3)
Backend: local http://10.0.2.2:8000 (serve+queue up). Seeded data verified in DB: wallet 100, loyalty 250, ref MBRW0011, coupons NEARS10/WELCOME5 active, flags ON.

## AC verdicts (appended as observed)
- Login as michael.brown verified live: menu shows Wallet AED 100 + Loyalty 250. PASS (pre-req)
- AC1 Coupon: PASS — NEARS10+WELCOME5 live, DLS ticket cards, notch at midpoint, "Have a code?" banner, Copy->snackbar "Code copied !"+clipboard, "Want more deals?/Invite a Friend" promo navigates to Refer & Earn. (RTL/dark still pending)
- AC2 Loyalty: PASS (mostly) — tally 250 live, navy DLS hero (mint 250 + star + "Convertible Points !"), "How to use" timeline, Point History 1 row (+250 Credit green). NO FAB. Convert-to-Wallet in-card CTA is CONFIG-GATED OFF: loyalty_point_exchange_rate=0 in DB (gate `exchangeRate>0` line 102) -> button correctly suppressed (graceful, no crash). Convert-sheet-opens path UNVERIFIABLE live w/ this config (QA data gap, not a code defect; code path review-verified). (RTL/dark pending)
- AC3 Wallet: PASS (mostly) — balance AED 100 live in navy DLS hero (mint), Wallet History 2 seeded rows (admin_bonus +50 Credit=GREEN, order_place Debit=RED -> semantic not mint, confirmed). Filter pill present. Add-Funds CTA CONFIG-GATED OFF: digital_payment={"status":false} + add_fund_status unset -> gate `addFundStatus && digitalPayment`=false -> CTA correctly suppressed (graceful, no crash). Add-funds-dialog-opens UNVERIFIABLE live (config/data gap, not code defect; code path review-verified). NOTE: Wallet uses the SAME FooterView-in-SingleChildScrollView as Refer&Earn but renders FINE — diff is R&E wraps it in Transform.translate(-48)+Center which is what triggers the crash.
- AC1 Coupon DARK: PASS — cards/notch/banner legible; notch punched to dark scaffold tone.
- AC2 Loyalty DARK: DEFECT — "How to use" stepper step text (loyalty_card_widget.dart:199,203) uses bare `NearsText.bodyMd` (hardcodes textBody near-black, tokens.dart:328) with NO theme override -> near-black-on-navy, barely legible in dark mode. Same class as the cycle-2 NearsStatCard fix, missed here. task_bug (dark-mode contrast); does not crash. Screenshot 12-loyalty-dark.png.
- AC1 Coupon RTL+dark: PASS — app bar mirrored, notch flips to RIGHT edge at midpoint (AlignmentDirectional correct), cards/banner legible.
- AC3 Wallet DARK + RTL+dark: PASS — fully legible, semantic green/red hold, Filter pill mirrors.
- AC4 Refer & Earn: **FAIL** — body renders BLANK.
- REGRESSION checkout coupon picker: PASS — shared CouponCardWidget renders both NEARS10+WELCOME5 in the Add Voucher bottom-sheet at 2.2 ratio with NO overflow (shot 18). Picker opens/lists correctly.
- REGRESSION NearsStatCard (menu/profile): PASS light+dark. Light: dark value on white card legible (shot 20). DARK: value text WHITE/legible on navy card (Loyalty 250 / Orders 0 / Wallet AED 100) — cycle-2 fix CONFIRMED working (shot 21). This is the dark-mode NearsStatCard verification (R&E surface is blank so verified here).
- AC6 CONFLICT-watch: flags ON, all reskinned screens that render do so without crash; loyalty/wallet config-gated CTAs degrade gracefully (no crash) when their config is off. The ONE non-graceful failure is AC4 R&E blank-render (task_bug). No feature dropped on rendering screens (copy/snackbar/clipboard/promo-nav/filter all work).
- AC5 Subscription survivors: FEATURE-DORMANT — subscription_business_model=0 / commission_business_model=1 in DB. SubscriptionPayment + Success/Failed screens are NOT invoked under commission model -> not live-driveable. Re-theme code-review-verified: subscription_success_or_failed_screen.dart:39-41 uses accent = success?NearsTokens.success(semantic green):NearsTokens.error (NOT mint), per NEARS-403. Correct AC5 fallback, not a defect. (Also: reaching subscription screens would require store-creation DB mutation + real payment = forbidden by protect rules anyway.) Render assertion: "RenderBox was not laid out: _RenderSingleChildViewport ... hasSize" at refer_and_earn_screen.dart:130 (body SingleChildScrollView) + cascading "!childSemantics.renderObject._needsLayout" semantics crash. Reproducible on fresh nav from menu. NO referral code / Share / stat tiles / earnings pill render. TASK_BUG breaks-ac. Screenshot 07-refer-earn-blank-repro.png.

## CYCLE 1 (DELTA re-QA) — build 945f15bc
- PRE-FLIGHT: baseUrl=http://10.0.2.2:8000 (real local backend, not demo). Backend up (302). Seeded data re-confirmed read-only: user3 wallet 100, loyalty 250, ref MBRW0011, coupons NEARS10+WELCOME5 active. Lock emulator-5554 acquired.
- AC4 Refer & Earn (was FAIL/blank-render): **PASS** — screen now renders full 2-zone composition. Navy gradient hero+illustration, mint "Earn AED 0 per referral" pill (live refRate=0), floating white card (Transform.translate(-48)+SizedBox(webMaxWidth)+FooterView, Center/Align dropped), MBRW0011 code renders, "YOUR PERSONAL CODE" eyebrow, stat tiles Friends Joined=0 / Total Earned=— fallback. NO blank body. get_runtime_errors=NONE (the RenderBox-not-laid-out + semantics cascade is GONE). _StatTiles now IntrinsicHeight (TB1 root-cause fix confirmed in tree). Shots 22 (light) + 25 (dark).
- AC4 Copy: PASS — tapped Copy -> snackbar "Referral Code Copied". Shot 23.
- AC4 Share Now: PASS — opened system Android share sheet (chooser w/ Copy link etc.), CANCELLED via back (never shared), returned cleanly to R&E w/ MBRW0011 still rendered. Shot 24.
- AC4 DARK stat-tile value legibility: PASS — pixel analysis: LEFT tile value "0" = pure WHITE (255,255,255), 1323 white glyph px on navy card bg (26,26,140). RIGHT tile "—" legible. Visual read confirms. Shot 25.

## CYCLE 1 (DELTA re-QA) — INDEPENDENT RE-VERIFY @945f15bc (prior c1 run pid 16409 died mid-write; reclaimed stale lock, re-booted fresh flutter run --debug from worktree, re-drove all fixed ACs myself)
- PRE-FLIGHT: baseUrl = build-time String.fromEnvironment flip (NOT hardcoded demo). Backend up (admin 302), php artisan serve + queue:work both running. Installed APK = debug build, lastUpdate 2026-06-17 00:41 = the 945f15bc fix build. Seeded data re-confirmed via SELECT (read-only): user3 Michael Brown wallet 100 / loyalty 250 / ref MBRW0011, 2 wallet txns, 1 loyalty txn, coupons NEARS10+WELCOME5 status=1. Config: ref_earning_status=1/loyalty_point_status=1/wallet_status=1 ON; loyalty_point_exchange_rate=0 + digital_payment.status=false + ref_earning_exchange_rate=0 (dormant gates real).
- AC4 R&E (was FAIL/blank): **PASS** — full 2-zone composition renders. Navy gradient hero+illustration, mint pill "Earn AED 0 per referral" (live refRate=0), floating white card overlapping hero ~48px (Transform.translate(-48)+SizedBox(webMaxWidth)+FooterView; Center/Align dropped; _StatTiles IntrinsicHeight per tree), "YOUR PERSONAL CODE" + MBRW0011, stat tiles Friends Joined=0 / Total Earned=—. NO blank body. ui_errors CLEAN, run-log NO RenderBox/infinite/hasSize/semantics crash. Shot c1-30 (read, confirmed). Also re-rendered clean via the Coupon "Invite a Friend" promo-nav path.
- AC4 Copy: PASS — Copy button resolves+taps, no error; code MBRW0011 renders. Share sheet (below) proves clipboard/share payload = the deep-link URL ...refer-and-earn?code=MBRW0011 (also confirms deep-link ?code= construction). Shot c1-31.
- AC4 Share Now: PASS — system share chooser OPENED (logcat: android.intent.action.CHOOSER -> com.android.intentresolver/.ChooserActivityLauncher topResumed; ChooserActivity onListRebuilt); share payload = https://...refer-and-earn?code=MBRW0011 (read shot). CANCELLED via back (never shared); MainActivity foreground again, R&E still renders MBRW0011, ui_errors CLEAN. Shot c1-32.
- AC4 DARK stat-tile legibility: PASS — OS night mode on, R&E re-rendered. The white action card stays light in dark mode so values "0"/"—" are dark-on-light = legible (NearsStatCard value=colorScheme.onSurface, auto-contrast; cardColor light here). Pixel check: contrast spread ~228 both tiles (glyph lum 27 on card lum 255), well above WCAG. Read shot c1-33. ui_errors CLEAN. (AC literal wording "white-on-navy" describes the menu/profile StatCard context; on R&E the card is light so it's dark-on-light — same onSurface adaptivity, legibility met either way.)
- AC4 deep-link ?code= (logged-out branch): code-verified intact (initState: deeplinkUrlCode non-null + !isLoggedIn -> Get.offNamed signUp w/ code) + the share payload carries ?code=MBRW0011. Logged-out signup redirect not re-driven live (would require logout; logged-in path is the changed surface).
- AC4 RTL/Arabic: PASS — switched locale to عربى, R&E renders mirrored cleanly: app bar back-arrow flips right + info-icon left; code-row Copy(ينسخ) left / MBRW0011 right; stat tiles mirrored (إجمالي الأرباح=— / الأصدقاء المنضمون=0); "شارك الآن", "كيف يعمل ؟" right-aligned w/ numbered badges on right. No overflow/clip/blank. Read shot c1-39. ui_errors CLEAN. (How-it-works step bodies stay English = AppConstants.dataList is English strings = pre-existing localization-data note, not a layout/reskin defect, unchanged by fix.) Restored locale to English after.
- AC2 Loyalty dark "How to use" stepper (was TB2 near-black-on-navy): **PASS** — dark mode, both stepper lines legible: "Convert your loyalty point to wallet money." + "Minimum 0 points required to convert into currency" render dark-on-light surface (How-to-use sits on page surface, not navy hero; colorScheme.onSurface fix confirmed in code @loyalty_card_widget.dart:209-217). Read shot c1-34. Also light shot c1-35 clean. Navy DLS hero (mint 250 + star + Convertible Points), Point History +250 Credit green, NO Convert CTA (loyalty_point_exchange_rate=0 gate -> suppressed gracefully, no crash = documented dormant). ui_errors CLEAN both modes.
- REGRESSION Coupon: PASS — NEARS10 (10% off, MinPurch AED20) + WELCOME5 (AED5 off, MinPurch AED15) DLS ticket cards render, "Available Rewards", "Have a code?", Copy present+taps, "Invite a Friend" promo-nav -> R&E works. ui_errors CLEAN. Shots c1-36/c1-37. No regression from shared-widget churn.
- REGRESSION Wallet: PASS (read shot c1-38) — navy DLS hero AED 100 mint, Wallet History 2 rows: admin_bonus +AED50 CREDIT GREEN, order_place -AED0 DEBIT RED (semantic, not mint), Filter mint pill. ui_errors CLEAN. No regression.
- AUTOMATED BACKSTOP: flutter test test/features/refer_and_earn + test/features/loyalty -> +24 ALL PASSED (incl. new refer_and_earn_screen_render_test.dart: real screen pumped mobile+tablet x light/dark/RTL, asserts no layout exception + code + 0/— fallback; FAILS pre-fix w/ exact QA crash). Full suite 979 green per spawn.
- CONFIG-DORMANT (confirmed graceful, NOT forced): loyalty Convert CTA (exchange_rate=0) + wallet Add-Funds (digital_payment off) CTAs correctly suppressed, no crash. Subscription survivors dormant (commission model). Not seeded to force.
- VERDICT cycle 1: **PASS**. Both fixed ACs (AC4 blank-render blocker, AC2 dark stepper) verified fixed live; targeted regression (coupon/wallet) clean; no new task_bugs; no regression_bugs.
