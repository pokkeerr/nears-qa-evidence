AC1 — UserApp/dart_test.yaml defines a `pin` tag alongside `golden`, with a description.

$ cat UserApp/dart_test.yaml
# Declares the `golden` tag that alchemist applies to golden tests, silencing
# the "tag wasn't specified" warning and letting CI target them explicitly:
#   flutter test --exclude-tags golden   # core gate (skips pixel goldens)
#   flutter test --tags golden           # DLS visual-regression goldens
tags:
  golden:
    description: "Pixel golden tests (alchemist). DLS visual-regression guard."
  pin:
    description: "PIN: characterization tests pinning current (incl. defective) behavior. NEARS-3084."

VERDICT: PASS — `pin` tag present, mirrors `golden` tag's shape (map with `description` key), short description of what it gates.
