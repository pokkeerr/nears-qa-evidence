NEARS-3289 — installer SQL dump secret scrub — QA evidence
Device-free ticket (backend data-file edit only). No emulator/browser surface.
Worktree: /Users/Apple/Projects/nears-NEARS-3289-installer-sql-secrets
Branch:   fix/NEARS-3289-installer-sql-secrets
File under test: Admin/installation/backup/database.sql (6283 lines)

== AC2 — oauth_clients placeholder secrets ==
grep for the 4 original leaked oauth_clients secret values against
Admin/installation/backup/database.sql: 0 hits (all 4).

Raw INSERT statement (file line 2575-2579):
  (1, NULL, 'Laravel Personal Access Client', 'f6R7pDYkkh6xTno1nxrq0xRo4wnM1HPJtdwMbYw0', NULL, 'http://localhost', 1, 0, 0, ...),
  (2, NULL, 'Laravel Password Grant Client', NULL, 'users', 'http://localhost', 0, 1, 1, ...),
  (3, NULL, 'stackfood Personal Access Client', 'n3tJaeCmbrYLRzqowQGwaQhEkz45GtrvAWmE4REZ', NULL, 'http://localhost', 1, 0, 0, ...),
  (4, NULL, 'stackfood Password Grant Client', NULL, 'users', 'http://localhost', 0, 1, 1, ...);

id=1: secret='f6R7pDYkkh6xTno1nxrq0xRo4wnM1HPJtdwMbYw0' (non-empty placeholder, not one of the 4 leaked values), revoked=0
id=2: secret=NULL, revoked=1  -- matches required Password Grant Client pattern
id=3: secret='n3tJaeCmbrYLRzqowQGwaQhEkz45GtrvAWmE4REZ' (non-empty placeholder, not one of the 4 leaked values), revoked=0
id=4: secret=NULL, revoked=1  -- matches required Password Grant Client pattern
VERDICT: PASS

== AC3 — Flutterwave keys emptied ==
grep for the 3 original leaked flutterwave key values against
Admin/installation/backup/database.sql: 0 hits (all 3).

addon_settings row (file line 121, gateway='flutterwave'):
  live JSON:    {"gateway":"flutterwave","mode":"live","status":1,"secret_key":"","public_key":"","hash":""}
  sandbox JSON: {"gateway":"flutterwave","mode":"live","status":1,"secret_key":"","public_key":"","hash":""}

business_settings row (file line 546, key='flutterwave'):
  (57, 'flutterwave', '{"status":1,"public_key":"","secret_key":"","hash":""}', ...)

All secret_key/public_key/hash fields are empty string "". VERDICT: PASS

== AC5 spot-check — gitleaks allowlist still covers this file ==
.gitleaks.toml:178   paths = ['''(Admin|backend)/installation/backup/database(_3\.6)?\.sql''']
.gitleaks-baseline.json:162  "paths": ["Admin/installation/backup/database.sql", "Admin/installation/backup/database_3.6.sql",
           "backend/installation/backup/database.sql", "backend/installation/backup/database_3.6.sql"]
Both still list Admin/installation/backup/database.sql. VERDICT: confirmed still true (not required, spot-checked only).

== Functional regression check — MySQL import ==
Created a throwaway scratch database (NOT the shared dev multi_food_db):
  mysql -u root -h 127.0.0.1 -P 3306 -e "CREATE DATABASE nears_qa_3289_scratch CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

Imported the edited file directly into it:
  mysql -u root -h 127.0.0.1 -P 3306 nears_qa_3289_scratch < Admin/installation/backup/database.sql
Result: exit 0, empty stderr (zero SQL errors). Import completed in ~2.6s.
Post-import table count: SHOW TABLES => 153 tables created.

Row-level sanity check in the scratch DB:
  SELECT id, secret, revoked FROM oauth_clients ORDER BY id;
  id=1  secret='f6R7pDYkkh6xTno1nxrq0xRo4wnM1HPJtdwMbYw0'  revoked=0
  id=2  secret=NULL                                         revoked=1
  id=3  secret='n3tJaeCmbrYLRzqowQGwaQhEkz45GtrvAWmE4REZ'  revoked=0
  id=4  secret=NULL                                         revoked=1
  -> matches the file-level pattern exactly (import round-tripped cleanly, no data loss/mangling).

  SELECT id, `key`, value FROM business_settings WHERE `key`='flutterwave';
  id=57 flutterwave {"status":1,"public_key":"","secret_key":"","hash":""}
  -> empty keys confirmed post-import as well.

Scratch DB dropped immediately after verification:
  mysql -u root -h 127.0.0.1 -P 3306 -e "DROP DATABASE nears_qa_3289_scratch;"  -- exit 0
  Confirmed absent from SHOW DATABASES afterward.

Note: full Laravel app boot / end-to-end OAuth login was explicitly out of scope per the
ticket brief (disproportionate environment setup for a data-file ticket). Import success +
row-level value verification is the evidence provided for the functional/regression AC,
as agreed in the ticket instructions.

Shared dev database (multi_food_db) was never touched — read-only project rule respected.

== AC1 (pre-confirmed by prior phase) ==
database.sql required for install; database_3.6.sql does not exist in this tree.
No independent action taken by QA — noted as confirmed-by-prior-phase per ticket brief.

== AC4 ==
Out of QA scope per ticket brief (Tier C owner-park, rotation decision). Not verified, not blocking.
