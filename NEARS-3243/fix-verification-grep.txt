28:                @php($language=\App\Models\BusinessSetting::where('key','language')->first())
29:                @php($language = $language->value ?? null)
30:                @php($defaultLang = str_replace('_', '-', app()->getLocale()))
253:                                        @php($campaign_nutritions = $campaign->nutritions->pluck('id'))
254:                                        @php($campaign_allergies = $campaign->allergies->pluck('id'))
418:                            @php($campaignAttributeIds = json_decode($campaign['attributes'], true))
419:                            @php($campaignAttributeIds = is_array($campaignAttributeIds) ? $campaignAttributeIds : [])
420:                            @php($campaignChoiceNo = json_decode($campaign['attributes']))
421:                            @php($campaignChoiceNo = is_array($campaignChoiceNo) ? $campaignChoiceNo : [])
422:                            @php($campaignChoiceOptions = json_decode($campaign['choice_options'], true))
423:                            @php($campaignChoiceOptions = is_array($campaignChoiceOptions) ? $campaignChoiceOptions : [])
0 @endphp tokens confirmed; 11 @php(...) single-statement directives
