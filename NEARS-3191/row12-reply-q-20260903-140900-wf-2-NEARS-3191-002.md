# q-20260903-140900-wf-2-NEARS-3191-002
- asker: wf-2 · ticket: NEARS-3191 · phase: 8 (AC e live round trip) · advisor: advisor-workflow (session ab416200, launched from the run worktree by QA at 14:06:43Z) · model: claude-fable-5-1 xhigh
- advisor sha: 067648b4b0f92c1277f9d95ec74de41039890f0f (worktree gitdir → refs/heads/feat/NEARS-3191-advisor-workflow-brief) == EXPECTED_SHA
- sent: 2026-09-03T14:09:00Z · deadline: 14:12:00Z · advisor idle: 14:18:37Z (577 s) · conductor received: 14:18:54Z (594 s) · timeout: TRUE (AC e row 13 bound 180 s)
- spot-check: PASS — (1) .claude/commands/new-task.md line 186 ends at the `--ours`/`--theirs` conflict rule, no post-merge assertion; (2) scripts/newtask-guards/worktree-prune.sh lines 289-290 KEEP+continue precede the merge-base --is-ancestor check at line 308; (3) jira-map.json AUD-WF-01 → NEARS-3140 Critical.

## QUESTION (verbatim)
Does `.claude/commands/new-task.md` §[9b] item 10 state a positive post-merge assertion (new primary HEAD != old, HEAD^2 == run branch tip, merge-base --is-ancestor)? If not, what is the only post-merge check [9b] performs, and which open Jira ticket covers the gap? Cite path + line range and the sha you read.

## ANSWER (verbatim)
No. `.claude/commands/new-task.md` §[9b] item 10 (line 186) states NO positive post-merge assertion. Line ends at `git merge --no-ff <this run's branch> -m "merge(<KEY>): <one-line summary>"` followed only by conflict rule ("Never a blind `--ours`/`--theirs`" … semantic conflict STOPS). No rc check, no new-base-sha != old, no `git rev-parse <new>^2 == branch tip`, no `merge-base --is-ancestor <branch> <base>`. Also no "never pipe the merge" clause.
Item 10's only checks are PRE-merge: re-confirm primary branch == run file `base`; base advanced → `git rebase <base>` in worktree, re-run step 5 and step 9(a)-(d). Every `merge-base --is-ancestor` in new-task.md is pre-merge and points the other way (base is ancestor of HEAD): line 101 ([6] base descent), line 154 ([9b] item 2).
Only post-merge check [9b] performs: item 11 (line 187) `worktree-prune.sh --keep <this run's branch>` — "reaps only fully-merged + clean worktrees". But the script KEEPs any `--keep` match BEFORE evaluating merge status (worktree-prune.sh lines 289-290), so its `merge-base --is-ancestor "$branch" "$BASE"` (line 308) is never reached for this run's own branch. Net: [9b] asserts nothing about whether THIS run's merge landed.
Next gate after [9b]: [11] item 3 (line 194) claims audit-crosscheck.sh verifies "the merge landed on the base", but `audit-crosscheck.sh` main() lines 142-158 only assert branch ref exists + base ref exists + INFO shortstat. That gap is AUD-WF-06 (NEARS-3145), not the one asked.
Open Jira ticket covering item 10 gap: NEARS-3140 = AUD-WF-01, Critical, epic NEARS-3123. Ledger status "Preparing" (live Jira status UNKNOWN — no tracker tool). Suggested fix text (findings.json line 58): old/new rev-parse, rc==0, new!=old, `$new^2` == branch tip, is-ancestor; never pipe the merge.

## EVIDENCE (verbatim, abridged headings only — full text in the conductor transcript)
new-task.md 186-189, 152-161, 100-102, 69-71, 191-198, 145-149 READ; negative greps (all hits read); worktree-prune.sh 285-324 READ; audit-crosscheck.sh 140-159 READ; findings.json 47-65 READ; jira-map.json 17-24 READ; summary.md 50-56, 180-182 READ; ledger srcid-aud-wf-01 READ via grep context; .git gitdir/HEAD/loose ref READ.

## CONFIDENCE (verbatim)
High, 0.92.

## CAVEATS (verbatim, abridged)
Read-only, nothing executed. NEARS-3140 live status UNKNOWN. Audit cites item 10 at line 184; at this sha it is line 186. rationale.md not read. worktree-prune.sh / audit-crosscheck.sh partially read; absence beyond the read ranges INFERRED from single-hit greps.

## CONDUCTOR DISPOSITION
Row 12 (envelope shape, correct citation incl. lines 186-187, SHA == EXPECTED_SHA) = PASS on merits. Row 13 (≤180 s) = FAIL, measured 577 s (advisor idle) / 594 s (received) — triaged per D6 as cross-ticket advisor-budget miscalibration (design §7), regression ticket under NEARS-3122, not a brief defect. Bonus finding volunteered by the advisor (worktree-prune --keep short-circuits the merged check for the run's own branch) → forwarded to audit-mgr as a note on NEARS-3140/3145.
