=== source checks for ANSWER claims
-- new-task.md L186 full, post-merge assertion words (rc|rev-parse|is-ancestor|^2) present?
   1 assert
(empty above = none)
-- L186 tail:
"merge(<KEY>): <one-line summary>"`. **Never a blind `--ours`/`--theirs`:** a ledger/status-type conflict takes whichever side is live-Jira-verified or structurally a superset; a real semantic code conflict **STOPS** and escalates `NEEDS_DECISION` to the user.
-- L187 starts with item 11 worktree-prune: 1
-- jira-map AUD-WF-01:
19:   "id": "AUD-WF-01",
20-   "key": "NEARS-3140",
21-   "severity": "Critical",
-- brief Answer contract required H2/shape (L166-203):
15:Reply with ONE SendMessage to the sender named in the incoming message's `from` attribute. The first line is `qid: <qid>`; the rest is the envelope below, headings in this order, nothing else. Answer envelope, design §7, verbatim — the a
18:## ANSWER          the answer, one name when one name was asked for
19:## EVIDENCE        every path plus line range READ this session; grep-only hits marked as such
20:## SHA             the commit read from .git/HEAD and its ref
21:## CONFIDENCE      High | Medium | Low, with a number, and what it rests on
22:## CAVEATS         edges, unverified assumptions, "read-only, nothing executed"
32:| Spot-check | before relaying, the conductor opens one cited path and line range from EVIDENCE and confirms it says what ANSWER claims; the result goes in the reply file (section 9) |
