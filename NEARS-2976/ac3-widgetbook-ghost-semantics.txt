NEARS-2976 AC3 — widgetbook NButton Playground, variant=ghost, isLoading knob toggled live
Captured via Playwright against the running `flutter run -d chrome` widgetbook dev server
(flt-semantics DOM nodes, semantics enabled via the flt-semantics-placeholder click).

IDLE (isLoading=false, positive control):
role= button | label= 'Go to Checkout' | aria-disabled= None

LOADING (isLoading=true, live toggle):
role= button | label= 'Go to Checkout, loading' | aria-disabled= true

This matches the fix's Semantics(container: true, excludeSemantics: true, button: true,
enabled: false, label: '<label>, loading') wrapper — button:true -> role=button,
enabled:false -> aria-disabled=true, label ends ", loading".
