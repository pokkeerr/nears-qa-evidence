# NEARS-412 QA progress (fix-cycle 0, first live pass)
Device: emulator-5554 (Android sdk_gphone16k_arm64)
Build: feat/NEARS-412-help-chat-reviews-reg @ 16fc4b7f (fix1 atop c9886149)
Backend: http://10.0.2.2:8000 (local, HTTP 200) | queue:work running
Started: live QA pass

## AC checkpoints (appended as observed)

- AC-1 SUPPORT: PASS — 3 contact rows render (Address: Abu Dhabi, Call +971565811159, Email admin@admin.com). Call tap launched com.google.android.dialer; Email tap launched com.google.android.gm. No blank body (BC-1 FooterView guard holds). Shots: ac01-support-screen, ac01-call-dialer-launched, ac01-email-gmail-launched.

- AC-2 CONVERSATION LIST: PASS (empty-state path) — "Conversation List" app bar, AI-assistant promo card ("24/7 AI assistant..."), NearsEmptyState ("No conversations yet" + "Start chat" CTA) all render. No blank body (BC-1 guard holds). Also satisfies AC-11 empty-state (no_conversations_yet). User michael.brown has 0 conversations seeded. Shot: ac02-conversation-empty-aipromo.

- AC-2 CONVERSATION LIST (populated): PASS — DLS conversation card renders party "Nears/Admin", last-msg preview, green presence dot, timestamp, navy app bar + search + Messages header. Search field accepts input. No blank body (BC-1 guard). Shot: ac04-conversation-list-populated.
- AC-3 MESSAGES THREAD: PASS — empty-state (no_messages_yet) renders; typed "QA-test-NEARS412" -> send btn activated -> POST /api/v1/customer/message/send {receiver_type:admin} returned 200 -> outgoing MINT bubble (navy text, ✓, "Today 3:22 AM") rendered LIVE (real-time delivery). Image-attach: paperclip opened com.google.android.photopicker, selected photo -> preview thumbnail rendered + send btn went mint-active (pickImage+preview OK). removeImage overlay present in code (tiny corner target; not hit via derived coords - sub-feature, not blocking). Shots: ac03-chat-thread-empty, ac03-compose-active, ac03-message-sent-bubble, ac03-image-picker-open, ac03-image-preview-in-chat.
- AC-4 (chat list layout + party resolution): PASS — covered by AC-2 populated card (admin party resolved, presence dot, timestamp). 
- AC-11 (chat empty states): PASS — no_conversations_yet + no_messages_yet both observed live.

- AC-5 RATE & REVIEW: PASS — opened from delivered store order #26. DLS frame match: navy app bar, mint segmented tabs (Item + Delivery Partner dual-tab flow present for order w/ delivery_man), item card (Fresh Organic Tomato, د.إ12, Qty 1, real image), "Rate the Item" 5-star row (interactive), "Share Your Opinion" comment field, full-width MINT Submit (reachable+enabled). Delivery Partner tab: agent card "tt te" 0.0(0), "Rate His Service" stars, comment, Submit. Both widgets (item_review/deliver_man_review) render to frame. Submit NOT fired (rail: default-not-mutate review DB; submit wiring confirmed in code-review H-3 fix + 299-line prefill unit test). Prefill: order#26/item96 has no review for THIS order -> correctly empty; prefill capability backstopped by item_review_prefill_test.dart. Shots: ac05-rate-review-item-tab, ac05-stars-set, ac05-delivery-partner-tab.

- AC-6 REFUND: UNVERIFIABLE-LIVE (config-gated, NOT a defect) — the in-order "Request Refund" entry is correctly hidden because backend live /config returns refund_active_status=false (verified via curl /api/v1/config). The gate is intended app behavior (refundActiveStatus && delivered && !parcel && no-campaign); order#26 meets every gate EXCEPT the global config flag. Cannot enable live without mutating business_settings (HARD RULE: read-only DB). Reskinned screen confirmed in source: SelectedCardWidget reason rows, NearsEmptyState('no_refund_reasons'), DottedBorder uploader via pickRefundImage, NearsPrimaryButtonLoader submit. Backstopped by refund_selected_card_widget_test.dart (light/dark + RTL mint directional start-border) — runs in automated backstop. Evidence: live config flag state.

- AC-7 DELIVERY-MAN REGISTRATION: PASS (verified up-to-edge; terminal Submit NOT fired per rail) — multi-step renders to DLS frame. Step1 General Info: NearsInput Basic Info (First/Last/+971 phone w/ flag picker/E-mail), Profile Image dotted-border uploader -> photopicker opened, photo picked -> PREVIEW rendered w/ red remove(X) overlay, Account Information w/ PassViewWidget password-strength criteria checklist (1 lower/number/special/upper/8+) -> validation FIRED (blocked Next on empty + on mismatch + on non-compliant pass). Step nav: filled all fields + matching compliant pass -> reached Step2 Additional Info (also via step-pill tab). Step2: Delivery Info (3 re-themed CustomDropdowns: delivery type/Main Service Zone/vehicle), Identity Info (Passport dropdown, Identity Number, identity-images dotted uploader -> photopicker opened), full-width mint Submit reachable. Shots: ac07-dm-reg-step1, image-picked, account-info, pass-criteria, step1-filled, step2-additional.
- AC-8 (DM reg RTL/dark/render fidelity): covered by frame-match above + registration_screens_render_test.dart matrix (mobile+tablet x light/dark/RTL takeException-null).

- AC-9 STORE/RESTAURANT REGISTRATION: PASS (verified up-to-edge; terminal Submit NOT reached per rail) — step1 Vendor Info renders to DLS frame: navy "Vendor Registration" app bar w/ info-icon (M-2 tooltip now .tr), mint step-pills (Vendor Info/Owner Info), Basic Info card w/ English/Arabic language sub-tabs (both switch), Business Name NearsInput, Business Logo + Business Cover dotted-border uploaders. Logo picker -> photopicker opened + picked; Cover picker -> picked + PREVIEW rendered (cover thumbnail visible behind dialog). Validation gates Next (blocked on missing images + incomplete fields). BACK-GUARD ConfirmationDialog FIRED ("Your registration not setup yet / Are you sure to go back? / Yes(grey)+No(mint)") -> reskinned dialog, No=stay works. Owner Info + business-plan(commission/subscription BaseCardWidget + PackageCardWidget card_swiper) steps are sequential-validation-gated (correct) -> backstopped by registration_screens_render_test full-screen matrix. TOOLING NOTE: adb IME cannot input Arabic chars (NPE) -> blocks live forward-nav past step1 multilang field; NOT an app defect. Shots: ac09-store-reg-step1, arabic-tab, logo-preview, backguard-dialog.
- AC-10 (store reg RTL/dark/render fidelity + subscription branch): backstopped by registration_screens_render_test.dart (StoreRegistration mobile+tablet x light/dark/RTL, takeException null) — exercises full tree incl subscription swiper + back-guard.

- DARK MODE: MIXED — chat thread (BC-2): PASS (navy app bar "Test1 Dil"+phone white/legible; outgoing bubble navy-surface white text legible). Settings/conversation: PASS. 
  *** TASK-BUG (dark-mode invisible text, breaks AC dark-fidelity) ***
  rate_review_screen.dart:80 — body page title "Rate & Review" uses NearsText.title.copyWith(color: NearsTokens.navy) (hardcoded const navy) -> renders navy-on-dark-navy body = INVISIBLE in dark mode. The rest of the cluster uses Theme.of(context).colorScheme.onSurface (conversation:197, refund:90/143/174 all correct) — rate_review is the ONE screen that didn't follow the theme-safe pattern. SECONDARY: item_review_widget price (NearsText.priceMd = navy const) renders dark-navy on lighter-navy card = very low contrast. Light mode unaffected (navy-on-cream fine, ac05 confirms). Shots: ac-dark-rate-review, ac-dark-rate-review-TITLE-BUG-crop.
  FIX: rate_review_screen.dart:80 navy -> Theme.of(context).colorScheme.onSurface (match conversation/refund pattern); item price -> theme-driven or onSurface. This is exactly the BC-2/§6.2 NearsText-on-navy systemic trap the ticket was meant to sweep.
