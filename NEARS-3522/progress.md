## Fix-cycle 1 delta re-QA (AC4)
- UserApp release build (scripts/build-release.sh UserApp, USE_HTTPS=true auto-added): built OK, installed, launched -> notification perm dialog -> Allow -> "No internet connection / Oops! / Try Again" real Flutter screen reached within ~2s of grant. No splash hang. Evidence: ac4-userapp-release-launched.png
- DeliveryApp release build (RELEASE_BASE_URL=https://qa-placeholder.nears.app): built OK, installed (after uninstalling prior debug-signed pkg, sig mismatch), launched -> notification perm dialog -> Allow -> "Couldn't connect to the server. Please try again." / "Delivery" / "Retry" real Flutter screen reached. No hang. Evidence: ac4-deliveryapp-release-launched.png
- VendorApp release build (RELEASE_BASE_URL=https://qa-placeholder.nears.app): built OK, installed, launched -> notification perm dialog -> Allow -> "Failed to load configuration. Check your connection." / "Vendor App" / "Try Again" real Flutter screen reached. No hang. Evidence: ac4-vendorapp-release-launched.png
- Fail-loud check: DeliveryApp and VendorApp with RELEASE_BASE_URL unset -> exit 1 in ~0.006s, clear message, before gradle invoked. VendorApp with http:// (non-https) value -> same fail-loud, exit 1, ~0.005s.
- Debug sanity: UserApp `flutter build apk --debug` succeeds (26.5s), installs, launches, reaches language-chooser onboarding screen normally -- unaffected by the release-signing fix (debug buildType untouched by build-release.sh).
- Device: emulator-5556, locked via qa-lock-guard, released clean. Throwaway keystore generated in scratch-keystore/, key.properties written per-app, both removed after use (gitignored, never committed).
