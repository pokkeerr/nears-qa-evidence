# QA progress — batch NEARS-345/322/342/341/359/360
Device: emulator-5554 (Android 17 / API 37). Build: fix/userapp-open-tickets @ 1f48871d. baseUrl http://10.0.2.2:8000 (real local backend).

## Checkpoints (appended as observed)
- NEARS-342 PASS — fresh-install (pm clear com.izzes.nears) + guest flow, no saved address. logcat: 0 "Address Catch exception"; 0 EXCEPTION CAUGHT. App renders home normally (Abu Dhabi current-loc). Evidence: 02-fresh-guest-home.png, 03-guest-home-reached.png.
  - RESIDUAL (regression_bug, pre-existing, out of 342 scope): splash_repository.dart:117/150 still log "Did not get shared Preferences address . Note: Null check operator used on a null value" (1x on boot) — same unguarded getString(...)! pattern NEARS-342 fixed in address_helper. Caught/benign (debugPrint only), no crash.
- NEARS-322 **FAIL (RTL)** / PASS (LTR). LTR English: 0 overflow across cold-open + multiple pull-to-refresh shimmer triggers. RTL/Arabic: Dart MCP runtime errors caught "A RenderFlex overflowed by 25 pixels on the right" at item_shimmer.dart:59 (the rating-placeholder Row width:30+~5+width:20=~55px), errorsSinceReload:5. Overflow did NOT reproduce in LTR. AC requires "shimmer placeholders intact in portrait AND in RTL" -> RTL criterion not met. Fix bounded the favourite column (line 68) but line-59 Row still overflows when RTL squeezes the inner Expanded (likely the physical `margin: right` at line 46 + RTL). Evidence: 08-nears322-OVERFLOW-rtl-shimmer.png. Note: logcat alone did NOT surface this (only Dart MCP get_runtime_errors did).
- NEARS-360 PASS (live, EN + AR):
  - (a) inline CartCountView stepper: EN qty1 -> "Remove", qty2 -> "Decrease quantity"; AR qty1 -> "يزيل", qty2 -> "تقليل الكمية". Both shown simultaneously on home (Spinach qty1, Salted Pretzels qty2). Evidence 04, 06.
  - (b) password eye toggle (login): EN obscured -> "Show password", revealed -> "Hide password" (localized, not hardcoded). Evidence 05.
  - NOTE: the CART SCREEN stepper uses NearsQtyStepper (nears_qty_stepper.dart:41/54) which has HARDCODED English 'Decrease quantity'/'Increase quantity' and no qty-based remove/decrease logic — NOT covered by NEARS-360's fix (which targeted CartCountView + custom_text_field). Pre-existing a11y gap = regression_bug.
  - Could NOT verify actual TalkBack AUDIO output (headless emulator) — labels verified via semantics tree only.
- NEARS-341 PASS — tapped featured banner (store-pointing, "Abu Dhabi Fresh Market"/"Organic Shop") on Grocery home -> navigated correctly into the Organic Shop store page with correct items/module context. 0 runtime errors, 0 module-selection/firstWhere errors from the selectModuleForBannerStore MVVM move. Evidence 12.
- NEARS-345 **FAIL** (task_bug — the fix itself crashes). Reached Checkout FROM cart (fromCart, login customer@nears.com), Review Items card. Tapping minus on a qty-1 item throws a RangeError INSTEAD of removing the line. CLEAN ISOLATED REPRO: cleared errors (errorsSinceReload:0) -> single tap on Review Items minus (qty-1 item) -> exactly ONE fresh RangeError:
    RangeError (length): Invalid value: Valid value range is empty: 0
    #1 _ReviewItemRow.build.<anonymous closure> (review_items_section.dart:133:42)  Handler: "onTap"
  Item stays visible at qty1 (removal never completes). Root cause: the fix's onDecrement closure reads cartController.cartList[cartIndex] LIVE (line 133); after removeFromCart empties the list, the stale index 0 hits an EMPTY list -> "range empty: 0". The sibling cart screen (cart_item_widget.dart) uses the SAME guard but reads the CAPTURED widget.cart.quantity, so it works (minus at qty1 -> cart empties cleanly, no error - verified live). Fix should read captured `cart`/`qty`, not cartList[cartIndex]. NEVER goes negative (guard is right) but the impl crashes. Only Dart MCP get_runtime_errors surfaced it (screenshots + plain logcat did not; 112 widget tests pass because they call removeFromCart directly, not via this widget's live-index gesture). Evidence 18 + nears345-rangeerror.txt.
- NEARS-359 PASS (intent) / SCOPE-CAVEAT — the two PATCHED widgets (ItemCampaignView, PopularItemView) are NOT mounted in the live home (grep: referenced nowhere except their own defs; home uses MostPopularItemView + FlashSaleViewWidget instead). Fix is code-correct (removed inline `controller: ScrollController()` from both — confirmed 0 refs now) and covered by widget tests. LIVE: the mounted home horizontal rails (MostPopular/Recommended/FlashSale) scroll smoothly H+V with 0 runtime/controller errors. Could NOT exercise the exact item_campaign_view/popular_item_view strips live (dead in current home composition). Evidence 13.
