## Security acceptance (SEC-1) — --strict-mcp-config placement 11:02:16Z

### lib.sh launch template line:
87:  ADVISORS_ARGV=(--bg -n "$1" --append-system-prompt-file "$2" --model claude-fable-5-1 --effort xhigh --permission-mode bypassPermissions --strict-mcp-config --allowedTools Read Grep Glob ListAgents SendMessage --disallowedTools Bash Edit Write MultiEdit NotebookEdit WebFetch WebSearch Agent "Standby.")
lib.sh matches: 1

### dry-run lines carrying the exact sequence:
count: 9  (expect 9)

### design §5 line 69 carries it:
1

### argv element right after bypassPermissions (sourced lib, advisor-backend):
--strict-mcp-config

### behavioural proof (live advisor has no mcp__* tools): PENDING PART 2 — conductor §7 exchange
