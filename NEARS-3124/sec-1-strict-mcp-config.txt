## SEC-1 / NEARS-3133 tool lock — RECAPTURE, delta re-QA cycle 1, 11:28:24Z, HEAD 076911573 (supersedes the 5aeff56d7 capture)

### lib.sh launch template line:
91:  ADVISORS_ARGV=(--bg -n "$1" --append-system-prompt-file "$2" --model claude-fable-5-1 --effort xhigh --permission-m
lib.sh matches (strict-mcp-config then --tools allowlist): 1

### dry-run lines carrying the full cycle-2 deny tail:
count: 9  (expect 9)
count with --strict-mcp-config --tools: 9 (expect 9)

### design line 70 carries both:
1

### EXECUTED argv (sourced lib, advisor-backend): elements after --strict-mcp-config, and the wildcard element unexpanded
--tools
argv element count: 47
wildcard element (literal, no shell expansion):
46:mcp__claude-in-chrome__*
last element: Standby.

### behavioural proof (live advisor holds ONLY Read Grep Glob ListAgents SendMessage ToolSearch; no mcp__*): PENDING PART B — conductor §7 probe
