## AC (f) — owner-queue.sh 11:01:44Z

### $ bash scripts/advisors/owner-queue.sh
Owner queues — tasks/advisors/00-advisors-design.md §10

Ratify   (read the comment; reply to override, or leave it):
  project = NEARS AND labels = advisor-decided

Unblock  (reply on the ticket with the answer; the run resumes):
  project = NEARS AND labels = waiting-on-owner AND status = Blocked

Run each with the Atlassian MCP tool searchJiraIssuesUsingJql (cloudId in .claude/workflow-profile.md §Tracker); this script makes no REST call and reads no credential.
rc=0

### verbatim checks (grep -c -F):
advisor-decided JQL: 1
waiting-on-owner JQL: 1
searchJiraIssuesUsingJql hint: 1

### live JQL result: run by the conductor (session nears-7b) before this QA pass — both JQLs accepted, 0 results each. QA has no JQL search tool; cited, not re-run.
