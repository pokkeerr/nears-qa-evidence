=== CONTROL A — old wipe path (must show GONE) ===
[A0] baseline present:
package:com.izzes.nears
[A1] adb uninstall (simulates the tool auto-uninstall on the next flutter run):
Success
[A2] pm list after uninstall (EXPECT empty = GONE):
GONE (empty) — wipe mechanism confirmed real; repro CAN fail
