# NEARS-1025 cycle-2 FAIL — Change Location pick still throws "Null check operator" (5th unguarded zoneData!)
#
# Repro (device emulator-5554, worktree branch feat/NEARS-1025-cart-add-zone-validation @ 65687870):
#   1. active address = Demo Zone Dhaka (ADDR46, zone1); add Banana (item3, store2 Fresh Mart, zone1) to cart
#   2. switch active address -> Abu Dhabi (ADDR45, zone2) ; store2 now out of coverage
#   3. Basket -> Proceed to Checkout -> out-of-zone recovery panel shows (AC10 ok)
#   4. Change Location -> sheet offers 'Home — Demo Zone — Dhaka' (address_list payload: zoneIds set, zoneData NULL)
#   5. pick Dhaka -> checkout RENDERS (getModuleData no longer crashes — prong-1 held) BUT the
#      delivery-charge/coupon subtree throws a _TypeError during the build window.
#
# ROOT CAUSE — a 5th unguarded `address.zoneData!` force-deref the cycle-2 fix MISSED:
#   PricingService.calculateOriginalDeliveryCharge (pricing_service.dart:672)
#     `for (ZoneData zData in address.zoneData!) { for (Modules m in zData.modules!) {...} }`
#   called from CheckoutController.calculateCheckoutPricing:1182 (which reads
#     `AddressHelper.getUserAddressFromSharedPref()!` at :1141)
#   invoked from the checkout build closure (checkout_screen.dart:319, inside GetBuilder<CouponController>@308).
#
#   Prong-2 `_resolveActiveAddressZoneData()` (getZone + persist) is ASYNC and does NOT gate the build.
#   The checkout build runs on frames BEFORE resolution+persist completes, so the shared-pref address
#   still has zoneData==null in that window -> line 672 `.zoneData!` throws. Once resolution persists
#   zoneData, later builds are clean (settled re-entry into checkout is 100% error-free, COD/pricing ok).
#   => Same crash CLASS the fix targets, moved one method over. Fix null-guarded 4 methods in
#      checkout_controller (getModuleData/checkCODActive/checkDigitalPaymentActive/checkZoneOfflinePaymentOnOff)
#      but NOT PricingService.calculateOriginalDeliveryCharge:672 (nor calculateDeliveryCharge, verify).
#
# Deterministic: fires on every pick of a null-zoneData saved address (async race). Framework-caught
# (partial red ErrorWidget on the delivery-charge subtree during the window), not a full red-screen like
# cycle-0/1, but still a "Null check operator used on a null value" thrown during the AC11 flow -> AC11-a
# gate ("NEVER a crash / NO Null check operator") NOT met.
----
The following _TypeError was thrown building GetBuilder<CouponController>(state: GetBuilderState<CouponController>#057d3):
Null check operator used on a null value

The relevant error-causing widget was:
  GetBuilder<CouponController>
  GetBuilder:file:///.../UserApp/lib/features/checkout/screens/checkout_screen.dart:308:24

When the exception was thrown, this was the stack:
#0      PricingService.calculateOriginalDeliveryCharge (package:sixam_mart/features/cart/domain/services/pricing_service.dart:672:46)
#1      CheckoutController.calculateCheckoutPricing (package:sixam_mart/features/checkout/controllers/checkout_controller.dart:1182:111)
#2      CheckoutScreenState.build.<anonymous closure>.<anonymous closure> (package:sixam_mart/features/checkout/screens/checkout_screen.dart:319:26)
#3      GetBuilderState.build (package:get/get_state_manager/src/simple/get_state.dart:212:26)
...
