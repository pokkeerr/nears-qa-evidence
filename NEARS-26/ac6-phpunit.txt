=== AC6: phpunit full suite (phpunit.xml) ===
Tests: 205, Assertions: 4292 — ALL GREEN (2 deprecation notices only, no failures).

OtpSendThrottleTest (6/6 pass):
  ✔ Customer forgot password is throttled after three sends
  ✔ Deliveryman forgot password is throttled after three sends
  ✔ Vendor forgot password is throttled after three sends
  ✔ Other identifiers and ips are not blocked
  ✔ Empty email does not collapse otp send key   <- fix-cycle-1 regression
  ✔ Login is not subject to otp send limiter
