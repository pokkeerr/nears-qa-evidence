# NEARS-2900 QA progress (live checkpoint)
- 13:55 lock emulator-5558 (nears_qa_wave56) acquired; backend :8000 = 200; baseUrl -> 10.0.2.2:8000 (real local backend).
- 13:57 baseline worktree /Users/Apple/Projects/nears-NEARS-2900-qa-baseline @ fe9b85df0 (feat/userapp-reskin2, no _pendingProgrammaticMove) built+installed as com.izzes.nears.nears_nears_2900_qa_baseline.
- 14:01 BASELINE FAB tap, camera already centred: 1/1 (no camera move => no idle) -> scope item 1 as written does not discriminate fix vs base. Recorded baseline-entryA-fab-tap.log.
- 14:02-14:12 emulator-5558 FOUND UNFIT for camera-idle QA: Google Maps SDK "Error requesting API token. StatusCode=UNAVAILABLE" + maps_core GWS timeouts; map renders blank; onCameraIdle NEVER fires (huge programmatic move 1,1->Dhaka on BASELINE still 1/1; swipes/motionevents produce onCameraMoveStarted only — pin stuck in 'moving' shape). AVD has PlayStore.enabled=no. Network itself VALIDATED, TCP443 to maps.googleapis.com OK.
- 14:04 `adb emu geo fix` does NOT register a new GPS location while no GPS listener is active (lastKnown stays); working lever = `cmd location providers add-test-provider gps` + set-test-provider-location (verified gps+fused last location = 1,1).
- 14:13 booting spare AVD Pixel_10_Pro_3 on port 5560 (PlayStore.enabled=true) to retry.
- Activity for flavored pkg is <pkg>/com.izzes.nears.MainActivity (NOT <pkg>/.MainActivity — nav-guide drift).
- 14:15 emulator-5560 (Pixel_10_Pro_3) booted as spare: LOW DISK 546MB -> reclaim -> 562MB (<800MB floor), INSTALL_FAILED_INSUFFICIENT_STORAGE; released, left running.
- 14:16 emulator-5562 (NEARS_2411_QA, PlayStore.enabled=true, 2.9GB free) booted + locked; Maps SDK OK, camera idle fires, gestures work (downward swipes must start OFF the centre pin overlay, e.g. x=400,y=2000).
- BASELINE fe9b85df0 @5562: map-open auto-fetch 2/2; displaced FAB tap 2 geocode/2 zone; (1,1) FAB tap 2/2 + toast=2 + zone404=2 + [ERR]=2 (baseline-*.log).
- FIX 5f8513d1b @5562: entryA map open 1/1; entryA displaced FAB 1/1 addr updated, Pick Location enabled; entryB (add_address > Map) displaced FAB 1/1 Pick Address enabled, hand-back shows picked addr; add_address Use-my-current-location 1/1; (1,1) FAB tap 1/1 toast=1 zone404=1 [ERR]=1 button Service-not-available disabled.
- Positive controls: FAB 1/1 -> drag 2/2 (addr changed) -> drag back 3/3 (addr changed); mid-animation grab 2/2 addr=drag end; rapid double-tap geocode=2 zone=1 (identical-coords in-flight dedup, pre-existing) no crash; centred tap+12s+drag 1->2->3; in-zone x5 5/5 button enabled after each settle.
- Slow network: `adb emu network speed gsm` does NOT throttle 10.0.2.2 host traffic (zone still ~0.7s). Used a local delay proxy (127.0.0.1:8931 -> :8000, --dart-define=API_HOST=10.0.2.2:8931 via FLUTTER_BIN wrapper): zone +6s in-zone -> button DISABLED 0-4s, ENABLED at ~6s when zone landed; (1,1) -> DISABLED throughout and after 404; counts 1/1; toast 1. NOTE: first proxied build briefly pointed at :8001 = nears-audit-2026-09 backend (port collision, read-only calls only) before moving to :8931.
- RTL ar: map open with stale lastKnown (>5min, mock provider emits no fresh fix) -> [FAIL] getPosition TimeoutException fallback (instrument artefact, paired log) -> 2 pairs from 2 sources (initial idle + fallback auto-fetch, NOT a duplicate); RTL displaced FAB 1/1, FAB on the LEFT, zoom a11y labels still English ('Zoom in'/'Zoom out').
- Regression: search pick sets addr + zone lookup only (by design). RC1 CONFIRMED (drag, 0.7s, FAB: drag's late geocode overwrites the FAB address; base self-healed via the duplicate lookup). RC2 CONFIRMED (geocode +6s: button enabled ~6s with previous address text).
- 14:52 teardown: app stopped, com.izzes.nears.nears_nears_2900_fab_double_lookup uninstalled, mock provider removed, proxy killed, lock released. flutter test test/features/location/: +224 All tests passed.
