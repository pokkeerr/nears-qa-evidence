$ source scripts/app-nav/uinav_ios.sh
$ sleep 5   # let post-launch startup burst settle
$ out="$(ui_errors_ios 6)"; rc=$?
RC=0
STDOUT=[]  (empty)
STDERR: ui_errors_ios: scanned 31 lines for process "Runner"; 0 match(es)
Context: booted iPhone 17 Pro simulator, UserApp (com.izzes.nears) live and
running (Runner pid attached), local backend reachable (curl 127.0.0.1:8000
-> 200 throughout). Window taken AFTER the app's own startup burst settled,
so this is a genuine live end-to-end 0-match scan, not a buffer replay.
