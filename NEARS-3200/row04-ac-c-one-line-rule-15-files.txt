.claude/agents/nears-ai-engineer.md:20:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-analytics-checker.md:44:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-backend-checker.md:22:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-backend-engineer.md:20:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-closeout-executor.md:34:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-content-checker.md:24:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-data-checker.md:20:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-frontend-engineer.md:19:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-merge-sentinel.md:22:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-product-owner.md:109:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-qa.md:39:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-reference-agent.md:25:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-security-reviewer.md:21:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-team-lead.md:19:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.
.claude/agents/nears-ux-designer.md:69:If you cannot proceed without a decision, return NEEDS_DECISION{question, options, recommendation, domain}; never ask the advisor yourself, never block on it.

md5 of extracted rule line, all 15 files: 0fb6f9311aab58d4184343081d526141 (uniq -c = 15 x one hash)
files defining an envelope (grep -l -i envelope): 15; files carrying the rule: 15
