33:- **verdict vocabulary:** `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · plus, for anyone: **`NEEDS_DECISION{question, options, recommendation, domain: backend|flutter|dls|product|workflow}`** — subagents cannot talk to the user, so **you relay the question** — a technical one to the roster advisor named by `domain` FIRST (Tier A, §"Advisors" below; its answer rides `decisions_already_made` in the next fix-cycle packet), and only a Tier C item to the owner (park Blocked + `waiting-on-owner`, never a chat wait) — then re-spawn with the answer. `BLOCKED{why}` covers a missing tool/environment, never guessed around.
