Branch tip moved during QA: 64bd5c62c -> 60433f8a4 (wip(NEARS-3200): checkpoint after 6-fix1). Delta = 1 file / 1 line:
diff --git a/docs/workflow/advisors/advisor-workflow.md b/docs/workflow/advisors/advisor-workflow.md
@@ -44 +44 @@ Each line ends with its source; a double-quoted clause is verbatim from that sou
-- Shared envelope `{phase, agent, verdict, summary≤3 lines, artifacts[], qa_points[], contract_impact: yes|no|n/a, followups[], drift[], confidence?}`; verdicts `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · `NEEDS_DECISION{question, options, recommendation}` for anyone — a technical one goes to the domain advisor first (Tier A), only a Tier C item to the owner; `BLOCKED{why}` covers a missing tool, "never guessed around". (.cl
+- Shared envelope `{phase, agent, verdict, summary≤3 lines, artifacts[], qa_points[], contract_impact: yes|no|n/a, followups[], drift[], confidence?}`; verdicts `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · `NEEDS_DECISION{question, options, recommendation, domain: backend|flutter|dls|product|workflow}` for anyone — a technical one goes to the roster advisor named by `domain` first (Tier A), only a Tier C item to the owner; `B

Re-run at 60433f8a4: row1 grep rc=1 (0 hits); row8 workflow-json-drift clean; row10 doc-citation-content --all --root PASS (71 citations); row12 H2 sets + line counts identical to base for all 6 brief files. advisor-workflow.md now has three reworded quoting lines (44, 60, 75) instead of two.
