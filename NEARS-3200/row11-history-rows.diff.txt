diff --git a/docs/workflow/new-task-rationale.md b/docs/workflow/new-task-rationale.md
index e10ba202b..8eb29fb6d 100644
--- a/docs/workflow/new-task-rationale.md
+++ b/docs/workflow/new-task-rationale.md
@@ -233,5 +233,7 @@ figure was derived from the 98 s spike alone; P50/P95 are recomputed by
 reported separately; early entries carry minute-precision estimates), never re-guessed, and a timeout
-stays a `drift[]` record rather than a stall. **Why every consult point is optional in the
-pilot:** design §12 compares the next 10 runs against the last 10 by lane; the points become
-mandatory only on a non-worse result across that whole table.
+stays a `drift[]` record rather than a stall. **Why advisors answer only questions (NEARS-3200):**
+the owner's rule — an advisor takes the decisions the workflow would otherwise put to the owner; a
+proactive per-phase review adds wall time to every run even when no question exists, so the only
+mandatory ask is the [2b] gate. Design §12 still compares the next 10 runs against the last 10 by
+lane; that comparison measures the pilot, it no longer promotes consult points to mandatory.
 
diff --git a/docs/workflow/task-creation-workflow.md b/docs/workflow/task-creation-workflow.md
index 37c5763c1..06dc417de 100644
--- a/docs/workflow/task-creation-workflow.md
+++ b/docs/workflow/task-creation-workflow.md
@@ -4,3 +4,3 @@
 > **Source of truth:** this file is mirrored to Confluence page **3113013**. The runnable spec is `.claude/commands/new-task.md` (per-ticket conductor) + `.claude/commands/run-sprint.md` (multi-ticket sprint runner) + `.claude/agents/*.md` (the agents). Conventions live in `CLAUDE.md` + `nears-reference.md`.
-> **Status:** LIVE (v2). Last updated 2026-09-03 (NEARS-3125 wired the advisors' consult points + decision tiers into `/new-task`, `/run-sprint`, the agent files and the profile — see Changelog). Prior: 2026-08-14 (NEARS-2083 added the [8b] fix-in-run triage outcome); 2026-08-14 (NEARS-1990 `--help` self-location convention + two RED guard suites fixed); 2026-07-16 (NEARS-1255 added `/run-sprint --team`/`--groups`); 2026-07-13 (NEARS-1219 regenerated the golden to byte parity + recorded no-guard governance); 2026-07-12 (NEARS-1144 closed 15 `/run-sprint`+`/new-task` orchestration gaps); 2026-07-08 (NEARS-1007 workflow gaps + PO/TL research; NEARS-1008 insights guardrails + /run-sprint; NEARS-1043 [7] contradiction check + optional confidence field).
+> **Status:** LIVE (v2). Last updated 2026-09-03 (NEARS-3200 made advisors question-only — see Changelog). Prior: 2026-09-03 (NEARS-3125 wired the advisors' consult points + decision tiers into `/new-task`, `/run-sprint`, the agent files and the profile); 2026-08-14 (NEARS-2083 added the [8b] fix-in-run triage outcome); 2026-08-14 (NEARS-1990 `--help` self-location convention + two RED guard suites fixed); 2026-07-16 (NEARS-1255 added `/run-sprint --team`/`--groups`); 2026-07-13 (NEARS-1219 regenerated the golden to byte parity + recorded no-guard governance); 2026-07-12 (NEARS-1144 closed 15 `/run-sprint`+`/new-task` orchestration gaps); 2026-07-08 (NEARS-1007 workflow gaps + PO/TL research; NEARS-1008 insights guardrails + /run-sprint; NEARS-1043 [7] contradiction check + optional confidence field).
 
@@ -313,3 +313,4 @@ Full how-to: `scripts/monitor/README.md`. The board's pipeline shape comes from
 
-- **2026-09-03 (NEARS-3125 — Advisors workflow integration: consult points + decision tiers wired into `/new-task`, `/run-sprint`, the agent files and the profile; design of record `tasks/advisors/00-advisors-design.md`)** — `.claude/commands/new-task.md` gained §"Advisors" (placed after §"Fix-cycle packet": the design §7 packet + answer envelope verbatim, the qid rule, the 3-min/5-min budget, the spot-check, the design §9 reply-file rule, the run file's new top-level `consults[]`, the packet's new `advisor_answers[]`, and the ONE A/B/C tier table — the item list cited from design §10, never restated) and re-routed its anchors by tier: [2b] is a tier gate (inside-scope approval incl. a narrowing/split = Tier B via `advisor-product` + label `advisor-decided`; widening = Tier C park Blocked + `waiting-on-owner`, worktree + run file kept), [4] Data's seeder-FILE approval moved to the data advisor's checklist (executing it against the shared DB stays Tier C), [8] the conductor consults the domain advisor on the compiled QA Test Scope BEFORE spawning QA (nears-qa holds no SendMessage; its `questions[]` route back through the conductor), [8b] cap-2 parks instead of asking in chat, and the envelope's `NEEDS_DECISION` bullet names the advisor as the first relay target. `run-sprint.md`: [3] plan approval inside the approved epic set = Tier B (the one consult the runner makes itself), outside it = Tier C; [4]'s pause list routed per trigger (the runner never consults an advisor for a per-ticket trigger; BLOCKED-resume mechanics stay design §14 item 4, unbuilt). The 10 agent files carrying "Inputs you expect" + `nears-team-lead.md` (which gained that section) each got one `questions[]`/`advisor_answers[]` line. Profile: Blocked (Jira `BLOCKED`, id 10044) + the two labels on the Statuses line; roster + reply-dir pointers under Run artifacts. `docs/workflow/workflow.json` + `tasks/workflow.json.golden`: `integrations.advisors` + rule `R14`, no node added/renamed/removed, golden re-synced byte-identical. `docs/workflow/new-task-rationale.md` §"Advisors" carries the WHY (design §3 measurements). Every consult point is optional in the pilot (design §12). Foundation: NEARS-3124 (`40415ef50`). Landed on `feat/NEARS-3125-advisors-workflow-integration`.
+- **2026-09-03 (NEARS-3200 — Advisors consult ONLY on questions)** — `.claude/commands/new-task.md` §"Advisors" "Consult points" cut to three triggers (a `NEEDS_DECISION` routed by `domain` · the [2b] gate · conductor ad hoc); the [2]/[6]/[8] proactive asks, the fix-cycle packet's advisor-answers field and the agent-envelope questions list removed; `NEEDS_DECISION` gains `domain: backend|flutter|dls|product|workflow`; the 15 agent files carry the one-line rule byte-identically; `docs/workflow/workflow.json` `integrations.advisors` + `tasks/workflow.json.golden` re-synced; `run-sprint.md` verified untouched (already NEEDS_DECISION-driven). Landed on `feat/NEARS-3200-advisors-questions-only`.
+- **2026-09-03 (NEARS-3125 — Advisors workflow integration: consult points + decision tiers wired into `/new-task`, `/run-sprint`, the agent files and the profile; design of record `tasks/advisors/00-advisors-design.md`)** — `.claude/commands/new-task.md` gained §"Advisors" (placed after §"Fix-cycle packet": the design §7 packet + answer envelope verbatim, the qid rule, the 3-min/5-min budget, the spot-check, the design §9 reply-file rule, the run file's new top-level `consults[]`, the packet's new advisor-answers field (removed by NEARS-3200), and the ONE A/B/C tier table — the item list cited from design §10, never restated) and re-routed its anchors by tier: [2b] is a tier gate (inside-scope approval incl. a narrowing/split = Tier B via `advisor-product` + label `advisor-decided`; widening = Tier C park Blocked + `waiting-on-owner`, worktree + run file kept), [4] Data's seeder-FILE approval moved to the data advisor's checklist (executing it against the shared DB stays Tier C), [8] the conductor consults the domain advisor on the compiled QA Test Scope BEFORE spawning QA (nears-qa holds no SendMessage; its questions route back through the conductor), [8b] cap-2 parks instead of asking in chat, and the envelope's `NEEDS_DECISION` bullet names the advisor as the first relay target. `run-sprint.md`: [3] plan approval inside the approved epic set = Tier B (the one consult the runner makes itself), outside it = Tier C; [4]'s pause list routed per trigger (the runner never consults an advisor for a per-ticket trigger; BLOCKED-resume mechanics stay design §14 item 4, unbuilt). The 10 agent files carrying "Inputs you expect" + `nears-team-lead.md` (which gained that section) each got one advisor questions/answers line (replaced by NEARS-3200's one-line NEEDS_DECISION rule). Profile: Blocked (Jira `BLOCKED`, id 10044) + the two labels on the Statuses line; roster + reply-dir pointers under Run artifacts. `docs/workflow/workflow.json` + `tasks/workflow.json.golden`: `integrations.advisors` + rule `R14`, no node added/renamed/removed, golden re-synced byte-identical. `docs/workflow/new-task-rationale.md` §"Advisors" carries the WHY (design §3 measurements). Every consult point is optional in the pilot (design §12). Foundation: NEARS-3124 (`40415ef50`). Landed on `feat/NEARS-3125-advisors-workflow-integration`.
 - **2026-08-14 (NEARS-2083 — third [8b] triage outcome: fix-in-run lane)** — Added a narrow third outcome to [8b]: a Low-severity, small finding (≤2 files, ≤20 changed lines, one surface, no design decision/new AC/new i18n key, inside the compiled QA Test Scope's blast radius, none of a 9-entry never-eligible veto list — `Backend` · auth/authz · money/wallet · data-loss · `contract-impact: yes` · migration · DB write · analytics event definition · new DLS component) is fixed inside the run instead of ticketed, under a **run cap** (max 3 items AND ≤50 changed lines combined), **conductor-only adjudication** (a flagging agent may propose, never self-execute), and a **forced delta re-QA with no carve-out**. Deliberately adds **no new `followups[]` type and no third [8b] drain** — the [2b] gate closed that gap by removing the originally-planned `fix-in-run-candidate` type outright (measured: `regression-candidate` has 14 producers, `dls-promotion-candidate` has 3, a new type would have had 0 — a producerless drain greps green exactly like a forgotten one); the predicate reads `severity` straight off QA's existing `task_bugs[]`/`regression_bugs[]` envelope fields, and a finding raised outside QA keeps the existing `regression-candidate` → QA-confirms → PO-files chain unchanged, this lane changes only its terminus. New top-level run-file array `fix_in_run[]` (`{lane, what, where, severity, files[], loc, why_eligible, verified_by}`, alongside `decisions[]`/`trail[]`/`sessions{}`, never inside `phases{}` — the same reviewer-key-blinding shape this repo has hit before) plus an [11] close-out heading **"Fixed in run (not separately ticketed)"**. `[9b]`'s scope guard gained a matching accommodation: when `fix_in_run[]` is non-empty the admitted commit set is the ticket's own scope **plus** exactly the paths in `fix_in_run[].files[]` — a union, never a replacement — via a new `commit-scope-check.sh --allow-exact <path>` flag (exact match only, never a prefix — `--allow-exact lib/a.dart` does not admit `lib/a.dart.bak`); the flag is **not wired** at `[9b]` today, same UNWIRED status as the sibling `precommit-scope-gate.sh`. `.claude/agents/nears-product-owner.md` gained a 1-line exception (an eligible `regression_bugs[]` item is diverted to fix-in-run and never filed by the PO); `CLAUDE.md`'s Atlassian-tracking mandate gained the same 1-line pointer carve-out. `docs/workflow/workflow.json` gained a `fixInRunNote` (shaped after the existing `sessionsMapNote`) plus the `[8b]` node's `hint`/`env.writes`; golden re-synced byte-identical. Canonical predicate text lives only in `.claude/commands/new-task.md` §[8b] — every other file (including this one) points at it, never restates it. Full detail: `docs/solutions/NEARS-2083-fix-in-run-lane.md`. Landed on `feat/NEARS-2083-fix-in-run-lane`.
diff --git a/tasks/advisors/00-advisors-design.md b/tasks/advisors/00-advisors-design.md
index ab8b2de8d..adf448a15 100644
--- a/tasks/advisors/00-advisors-design.md
+++ b/tasks/advisors/00-advisors-design.md
@@ -131,3 +131,3 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
 - phase: 6
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
 - files_in_play: [paths the phase is touching]
@@ -263,3 +263,3 @@ All edits below are made in one Cross-cutting workflow-meta ticket, built by `ne
 | same | `[4] Unblock`, the Data bullet ("after user approval") | seeder/migration FILE approval moves to the data advisor checklist (Tier A/B); execution against the shared database stays Tier C |
-| same | §"Fix-cycle packet" | the packet gains `advisor_answers[]`: qid, reply path, one-line answer |
+| same | §"Fix-cycle packet" | the packet gained an advisor-answers field: qid, reply path, one-line answer (removed by NEARS-3200 — answers ride `decisions_already_made`) |
 | same | §"Run state" (the `tasks/runs/<KEY>.json` shape at [1]) | the run file gains top-level `consults[]` (section 9) |
@@ -267,3 +267,3 @@ All edits below are made in one Cross-cutting workflow-meta ticket, built by `ne
 | same | `[4]` bullet "Pause and ask the user ONLY on" | rewritten to the tier rules per trigger: technical `NEEDS_DECISION` to the advisor; cap-2, sentinel BLOCKED, batch blocker, falsified premise per section 10; the runner never consults an advisor directly, only the per-ticket conductor does |
-| `.claude/agents/nears-backend-engineer.md`, `nears-frontend-engineer.md`, `nears-qa.md`, and the other seven agent files with an "Inputs you expect" section | `## Inputs you expect (in the spawn prompt)` | one line: raise domain questions as `questions[]` in the envelope and expect `advisor_answers[]` in the packet |
+| `.claude/agents/nears-backend-engineer.md`, `nears-frontend-engineer.md`, `nears-qa.md`, and the other seven agent files with an "Inputs you expect" section | `## Inputs you expect (in the spawn prompt)` | one line: raise domain questions in the envelope and expect the answers in the packet (superseded by NEARS-3200's one-line NEEDS_DECISION rule) |
 | `.claude/agents/nears-team-lead.md` | add an `## Inputs you expect` section (the same heading the other ten agent files carry), placed before its process steps | the same one line |
@@ -273,2 +273,3 @@ All edits below are made in one Cross-cutting workflow-meta ticket, built by `ne
 | `scripts/advisors/` | new | `up.sh`, `down.sh`, `owner-queue.sh`, and their self-test |
+| `.claude/commands/new-task.md` (2026-09-03, NEARS-3200) | §"Advisors", "Consult points" | consult points reduced to three question-driven triggers (a `NEEDS_DECISION` routed by `domain` · the [2b] gate · conductor ad hoc); the envelope questions list and the packet advisor-answers field removed; `NEEDS_DECISION` gains `domain`; the 15 agent files carry one identical line |
 
@@ -276,3 +277,3 @@ All edits below are made in one Cross-cutting workflow-meta ticket, built by `ne
 
-The pilot runs with the phase 1 roster: advisor-backend, advisor-flutter, advisor-dls, advisor-product. During the pilot the consult points are optional; a conductor may skip one and record the skip.
+The pilot runs with the phase 1 roster: advisor-backend, advisor-flutter, advisor-dls, advisor-product. During the pilot an ask outside the [2b] gate is optional; a conductor may answer a Tier A item itself and record the skip.
 
