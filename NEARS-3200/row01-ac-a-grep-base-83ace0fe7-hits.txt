.claude/agents/nears-merge-sentinel.md:22:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-team-lead.md:19:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-team-lead.md:31:5. **Solution doc — LANE-SCOPED (NEARS-2108). The lane decides the MEDIUM; the design decision is recorded either way, never dropped.** Your advisor consult (NEARS-3125) happens BEFORE this doc is written — so a design-shaping `questions[]` item goes out in an early envelope and the doc is written on re-engagement, because a doc written on a guess is the drift the consult exists to prevent.
.claude/agents/nears-content-checker.md:24:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-backend-engineer.md:20:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-frontend-engineer.md:19:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-qa.md:39:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-data-checker.md:20:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-security-reviewer.md:21:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-ai-engineer.md:20:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-backend-checker.md:22:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/agents/nears-reference-agent.md:25:**Advisors (NEARS-3125, pilot):** raise domain questions as `questions[]` in your envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and expect the answers back as `advisor_answers[]` in the packet; **never wait for an answer yourself** (you hold no SendMessage; the conductor asks — `.claude/commands/new-task.md` §"Advisors").
.claude/commands/new-task.md:33:- **verdict vocabulary:** `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · plus, for anyone: **`NEEDS_DECISION{question, options, recommendation}`** — subagents cannot talk to the user, so **you relay the question** — a technical one to the domain advisor FIRST (Tier A, §"Advisors" below; its answer rides the packet's `advisor_answers[]`), and only a Tier C item to the owner (park Blocked + `waiting-on-owner`, never a chat wait) — then re-spawn with the answer. `BLOCKED{why}` covers a missing tool/environment, never guessed around.
.claude/commands/new-task.md:128:**[8] QA — live verification.** → `nears-qa`. Status → In QA. **Before spawning QA, YOU ask the domain advisor about the compiled QA Test Scope** (5-min budget; optional in the pilot — §"Advisors") and fold the answer into the scope QA receives; `nears-qa` holds no SendMessage, so its own `questions[]` on return are answered through you into the delta re-QA packet's `advisor_answers[]`. Pass the AC + compiled QA Test Scope + `{worktree, branch}` + fix-cycle number. QA acquires a device from the profile's capped pool (absolute-path locks), boots the real surface, **demonstrates every AC live**, verifies specified analytics events fire (PII-safe), regression-sweeps the blast-radius list (bounded by the QA Test Scope), runs the automated backstop, publishes evidence per the profile (GitHub gallery — never tracker attachments). **QA posts its OWN QA-evidence comment on the ticket** (verdict + per-AC table + gallery link). Returns PASS/FAIL + evidence + findings in two lanes + `posted_comment`. **If `posted_comment:false`**, the conductor posts that comment in QA's stead. **Re-QA runs in delta mode** (failed ACs + targeted sweep only), re-spawned with the **FIX-CYCLE PACKET** (§"Fix-cycle packet" below).
.claude/commands/new-task.md:213:- advisor_answers: [{qid, reply_path, answer_one_line}] — the advisor consults answered
.claude/commands/new-task.md:227:**Consult points.** [2] the TL's `questions[]`, answered BEFORE it writes the solution doc · [6] the engineer packet (`questions[]` from the build envelope → `advisor_answers[]` in the next packet) · [8] the compiled QA Test Scope — asked by YOU to the domain advisor BEFORE spawning QA and folded into the scope QA receives; QA's own `questions[]` on return route through you into the delta re-QA packet · ad hoc, whenever you hold a domain question yourself. Any agent may return `questions[]` in its envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and never waits for the answer itself.
.claude/commands/new-task.md:235:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
.claude/commands/new-task.md:258:`sent_at` is DERIVED from the qid's embedded timestamp (`q-YYYYMMDD-HHMMSS-…` is authoritative; qid timestamps are UTC), `received_at` is a clock read at write time, and the file's mtime is the fallback when `received_at` is absent; then the remaining header items (asker, sha, model, spot-check result, timeout flag) · the question verbatim · the answer verbatim — never committed, outside every worktree. Then append the `consults[]` entry (§"Run state") + one `trail[]` line, and hand the answer on as `advisor_answers[]: [{qid, reply_path, answer_one_line}]` in the next fix-cycle packet.
docs/workflow/advisors/advisor-product.md:117:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-dls.md:122:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-workflow.md:60:- Every re-spawn on a fix cycle carries the FIX-CYCLE PACKET `{cycle, your_previous_work, findings_to_fix, decisions_already_made, open_items, advisor_answers}` built from the run file, "never from memory"; the checkpoint fires again with `--label <phase>-fix<N>`; an [8b] task-bug fix envelope carries `change_class: code|comment-only|doc-only`. (.claude/commands/new-task.md §Fix-cycle packet (conductor → any re-spawned or re-engaged agent))
docs/workflow/advisors/advisor-workflow.md:175:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-flutter.md:133:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-backend.md:147:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/brief-template.md:72:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
tasks/advisors/00-advisors-design.md:132:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
tasks/advisors/00-advisors-design.md:264:| same | §"Fix-cycle packet" | the packet gains `advisor_answers[]`: qid, reply path, one-line answer |
tasks/advisors/00-advisors-design.md:268:| `.claude/agents/nears-backend-engineer.md`, `nears-frontend-engineer.md`, `nears-qa.md`, and the other seven agent files with an "Inputs you expect" section | `## Inputs you expect (in the spawn prompt)` | one line: raise domain questions as `questions[]` in the envelope and expect `advisor_answers[]` in the packet |
docs/workflow/workflow.json:59:            "asker": "the conductor only — no phase agent holds SendMessage/ListAgents; nears-qa's questions[] route back through the conductor into the delta re-QA packet",
docs/workflow/workflow.json:61:                "[2] the TL's questions[], answered before it writes the solution doc",
docs/workflow/workflow.json:62:                "[6] the engineer packet (questions[] from the build envelope -> advisor_answers[] in the next packet)",
docs/workflow/workflow.json:67:            "packetField": "advisor_answers[]: [{qid, reply_path, answer_one_line}] in the fix-cycle packet",
docs/workflow/workflow.json:68:            "envelopeField": "questions[]: [{question, domain: backend|flutter|dls|product, files_in_play[]}] — optional on any agent envelope; the agent never waits for the answer",
docs/workflow/task-creation-workflow.md:314:- **2026-09-03 (NEARS-3125 — Advisors workflow integration: consult points + decision tiers wired into `/new-task`, `/run-sprint`, the agent files and the profile; design of record `tasks/advisors/00-advisors-design.md`)** — `.claude/commands/new-task.md` gained §"Advisors" (placed after §"Fix-cycle packet": the design §7 packet + answer envelope verbatim, the qid rule, the 3-min/5-min budget, the spot-check, the design §9 reply-file rule, the run file's new top-level `consults[]`, the packet's new `advisor_answers[]`, and the ONE A/B/C tier table — the item list cited from design §10, never restated) and re-routed its anchors by tier: [2b] is a tier gate (inside-scope approval incl. a narrowing/split = Tier B via `advisor-product` + label `advisor-decided`; widening = Tier C park Blocked + `waiting-on-owner`, worktree + run file kept), [4] Data's seeder-FILE approval moved to the data advisor's checklist (executing it against the shared DB stays Tier C), [8] the conductor consults the domain advisor on the compiled QA Test Scope BEFORE spawning QA (nears-qa holds no SendMessage; its `questions[]` route back through the conductor), [8b] cap-2 parks instead of asking in chat, and the envelope's `NEEDS_DECISION` bullet names the advisor as the first relay target. `run-sprint.md`: [3] plan approval inside the approved epic set = Tier B (the one consult the runner makes itself), outside it = Tier C; [4]'s pause list routed per trigger (the runner never consults an advisor for a per-ticket trigger; BLOCKED-resume mechanics stay design §14 item 4, unbuilt). The 10 agent files carrying "Inputs you expect" + `nears-team-lead.md` (which gained that section) each got one `questions[]`/`advisor_answers[]` line. Profile: Blocked (Jira `BLOCKED`, id 10044) + the two labels on the Statuses line; roster + reply-dir pointers under Run artifacts. `docs/workflow/workflow.json` + `tasks/workflow.json.golden`: `integrations.advisors` + rule `R14`, no node added/renamed/removed, golden re-synced byte-identical. `docs/workflow/new-task-rationale.md` §"Advisors" carries the WHY (design §3 measurements). Every consult point is optional in the pilot (design §12). Foundation: NEARS-3124 (`40415ef50`). Landed on `feat/NEARS-3125-advisors-workflow-integration`.
docs/workflow/advisors/advisor-product.md:117:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-dls.md:122:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-workflow.md:60:- Every re-spawn on a fix cycle carries the FIX-CYCLE PACKET `{cycle, your_previous_work, findings_to_fix, decisions_already_made, open_items, advisor_answers}` built from the run file, "never from memory"; the checkpoint fires again with `--label <phase>-fix<N>`; an [8b] task-bug fix envelope carries `change_class: code|comment-only|doc-only`. (.claude/commands/new-task.md §Fix-cycle packet (conductor → any re-spawned or re-engaged agent))
docs/workflow/advisors/advisor-workflow.md:175:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-flutter.md:133:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/advisor-backend.md:147:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
docs/workflow/advisors/brief-template.md:72:- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
