diff --git a/docs/workflow/advisors/advisor-backend.md b/docs/workflow/advisors/advisor-backend.md
@@ -147 +147 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
diff --git a/docs/workflow/advisors/advisor-dls.md b/docs/workflow/advisors/advisor-dls.md
@@ -122 +122 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
diff --git a/docs/workflow/advisors/advisor-flutter.md b/docs/workflow/advisors/advisor-flutter.md
@@ -133 +133 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
diff --git a/docs/workflow/advisors/advisor-product.md b/docs/workflow/advisors/advisor-product.md
@@ -117 +117 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
diff --git a/docs/workflow/advisors/advisor-workflow.md b/docs/workflow/advisors/advisor-workflow.md
@@ -60 +60 @@ Each line ends with its source; a double-quoted clause is verbatim from that sou
-- Every re-spawn on a fix cycle carries the FIX-CYCLE PACKET `{cycle, your_previous_work, findings_to_fix, decisions_already_made, open_items, advisor_answers}` built from the run file, "never from memory"; the checkpoint fires again with `--label <phase>-fix<N>`; an [8b] task-bug fix envelope carries `change_class: code|commen
+- Every re-spawn on a fix cycle carries the FIX-CYCLE PACKET `{cycle, your_previous_work, findings_to_fix, decisions_already_made, open_items}` built from the run file, "never from memory"; the checkpoint fires again with `--label <phase>-fix<N>`; an [8b] task-bug fix envelope carries `change_class: code|comment-only|doc-only`.
@@ -75 +75 @@ Each line ends with its source; a double-quoted clause is verbatim from that sou
-- The conductor is the ONLY asker: no phase agent holds `SendMessage`/`ListAgents`; an advisor holds no Write and no Bash. "Pilot rule (design §12): every consult point is OPTIONAL." — a skip is `advisor_skipped{phase, reason}` in `trail[]`, a timeout is `advisor_timeout{qid, advisor}` in `drift[]`; "Never wait past the budget.
+- The conductor is the ONLY asker: no phase agent holds `SendMessage`/`ListAgents`; an advisor holds no Write and no Bash. "Pilot rule (design §12): outside the [2b] gate an ask is OPTIONAL" — a skip is `advisor_skipped{phase, reason}` in `trail[]`, a timeout is `advisor_timeout{qid, advisor}` in `drift[]`; "Never wait past the
@@ -175 +175 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
diff --git a/docs/workflow/advisors/brief-template.md b/docs/workflow/advisors/brief-template.md
@@ -72 +72 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
