diff --git a/.claude/commands/new-task.md b/.claude/commands/new-task.md
index 707d2151e..50907f67f 100644
--- a/.claude/commands/new-task.md
+++ b/.claude/commands/new-task.md
@@ -21,7 +21,7 @@ At [1], create **`tasks/runs/<KEY>.json`** (you are its only writer): `{key, ide
 
 **`fix_in_run[]` — TOP LEVEL, alongside `decisions[]`/`trail[]`/`sessions{}`, never inside `phases{}`.** You are its only writer; empty is the normal state. One entry per finding the [8b] fix-in-run lane diverted: `{lane: "task_bugs"|"regression_bugs", what, where, severity, files[], loc, why_eligible, verified_by}` — `files[]` the paths the fix touched (the [9b] scope confirmation reads them), `loc` the changed-line count the ≤20/≤50 caps measure, `why_eligible` the conditions it cleared, **`verified_by` the delta re-QA that covered it** (a phase id or a `qa.ac_table[]` row).
 
-**`consults[]` — TOP LEVEL, alongside `fix_in_run[]`, never inside `phases{}` (NEARS-3125).** You are its only writer; empty is the normal state (every consult point is optional during the pilot). One entry per advisor question, design §9's field set verbatim: `{qid, advisor, phase, sent_epoch, received_epoch, elapsed_s, path, spot_check, timeout}` plus an optional `question` — `elapsed_s` the integer `received_epoch - sent_epoch` (conductor-received, `null` when unanswered), `path` the `~/.nears/advisors/replies/<qid>.md` file YOU wrote, `spot_check: PASS|FAIL`, `timeout: true|false` — plus one `trail[]` line per consult pointing at that path. Protocol, budget and tiers: §"Advisors" below.
+**`consults[]` — TOP LEVEL, alongside `fix_in_run[]`, never inside `phases{}` (NEARS-3125).** You are its only writer; empty is the normal state (empty when no NEEDS_DECISION was raised). One entry per advisor question, design §9's field set verbatim: `{qid, advisor, phase, sent_epoch, received_epoch, elapsed_s, path, spot_check, timeout}` plus an optional `question` — `elapsed_s` the integer `received_epoch - sent_epoch` (conductor-received, `null` when unanswered), `path` the `~/.nears/advisors/replies/<qid>.md` file YOU wrote, `spot_check: PASS|FAIL`, `timeout: true|false` — plus one `trail[]` line per consult pointing at that path. Protocol, budget and tiers: §"Advisors" below.
 
 **Validate after every write.** Immediately after each Edit that appends a phase envelope into `tasks/runs/<KEY>.json`, run `jq . tasks/runs/<KEY>.json > /dev/null` (fallback if jq is unavailable: `python3 -c "import json; json.load(open('tasks/runs/<KEY>.json'))"`). A failed parse **stops the phase** — fix the JSON (most commonly a missing comma at the boundary of the just-inserted phase object) before doing anything else. This catches the NEARS-1478 corruption at the write that caused it, instead of at the next resume or audit.
 
@@ -30,7 +30,7 @@ At [1], create **`tasks/runs/<KEY>.json`** (you are its only writer): `{key, ide
 ## Shared envelope — every agent returns exactly one
 Every agent ends its reply with a single fenced JSON envelope (its file defines role-specific fields):
 `{phase, agent, verdict, summary≤3 lines, artifacts[], qa_points[], contract_impact: yes|no|n/a, followups[], drift[], confidence?: high|medium|low}`
-- **verdict vocabulary:** `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · plus, for anyone: **`NEEDS_DECISION{question, options, recommendation}`** — subagents cannot talk to the user, so **you relay the question** — a technical one to the domain advisor FIRST (Tier A, §"Advisors" below; its answer rides the packet's `advisor_answers[]`), and only a Tier C item to the owner (park Blocked + `waiting-on-owner`, never a chat wait) — then re-spawn with the answer. `BLOCKED{why}` covers a missing tool/environment, never guessed around.
+- **verdict vocabulary:** `READY|MISSING` (checkers) · `PASS|FAIL` (QA) · `PASS|FINDINGS|BLOCKED` (reviewers) · `DONE|BLOCKED` (engineers) · plus, for anyone: **`NEEDS_DECISION{question, options, recommendation, domain: backend|flutter|dls|product|workflow}`** — subagents cannot talk to the user, so **you relay the question** — a technical one to the roster advisor named by `domain` FIRST (Tier A, §"Advisors" below; its answer rides `decisions_already_made` in the next fix-cycle packet), and only a Tier C item to the owner (park Blocked + `waiting-on-owner`, never a chat wait) — then re-spawn with the answer. `BLOCKED{why}` covers a missing tool/environment, never guessed around.
 - `drift[]`: anything the agent saw that contradicts the profile or its own definition — surface it to the user at close-out.
 - **`confidence` (optional):** set only when the agent hit a genuine judgment call or an edge case it couldn't fully verify. Omitting it is valid and free.
 - **Input packets:** pass what upstream already produced — issue key + AC/scope, the TL's change summary, `{branch, worktree, base, changed_files}` for reviewers/QA/sentinel, fix-cycle number on re-runs. Agents must not re-discover what's in the run file. **Lean context:** pass the envelope/summary, not raw transcripts; point agents at the relevant `docs/` topic file, **never the whole `nears-reference.md`**; for blast-radius use `graphify explain "<Symbol>"` over full-tree reads; keep your own context bounded by recording envelopes to the run file and not re-reading them. Leanness = no *redundant* re-discovery, **never** starving a phase of context it needs — if an agent would do worse without something, include it.
@@ -125,7 +125,7 @@ This is purely additive: it adds a NEW trigger point for the pre-existing RESUME
 - **Ledger promotions (Rule 6a) — conductor persists.** Drain `followups[].ledger-promotion` from every reviewer envelope and persist each to `docs/workflow/review-lessons.json` (grep the id, Edit that entry in place — never whole-file read; add at `seen:1, status:"candidate"` if new, else `seen++` and set `status:"active"` at `seen >= 2`; then validate with `jq .`). Only `status:"active"` rules bind engineers.
 - *Why — `docs/workflow/new-task-rationale.md` §"Lifecycle — [7] Review fan-out".*
 
-**[8] QA — live verification.** → `nears-qa`. Status → In QA. **Before spawning QA, YOU ask the domain advisor about the compiled QA Test Scope** (5-min budget; optional in the pilot — §"Advisors") and fold the answer into the scope QA receives; `nears-qa` holds no SendMessage, so its own `questions[]` on return are answered through you into the delta re-QA packet's `advisor_answers[]`. Pass the AC + compiled QA Test Scope + `{worktree, branch}` + fix-cycle number. QA acquires a device from the profile's capped pool (absolute-path locks), boots the real surface, **demonstrates every AC live**, verifies specified analytics events fire (PII-safe), regression-sweeps the blast-radius list (bounded by the QA Test Scope), runs the automated backstop, publishes evidence per the profile (GitHub gallery — never tracker attachments). **QA posts its OWN QA-evidence comment on the ticket** (verdict + per-AC table + gallery link). Returns PASS/FAIL + evidence + findings in two lanes + `posted_comment`. **If `posted_comment:false`**, the conductor posts that comment in QA's stead. **Re-QA runs in delta mode** (failed ACs + targeted sweep only), re-spawned with the **FIX-CYCLE PACKET** (§"Fix-cycle packet" below).
+**[8] QA — live verification.** → `nears-qa`. Status → In QA. Pass the AC + compiled QA Test Scope + `{worktree, branch}` + fix-cycle number. QA acquires a device from the profile's capped pool (absolute-path locks), boots the real surface, **demonstrates every AC live**, verifies specified analytics events fire (PII-safe), regression-sweeps the blast-radius list (bounded by the QA Test Scope), runs the automated backstop, publishes evidence per the profile (GitHub gallery — never tracker attachments). **QA posts its OWN QA-evidence comment on the ticket** (verdict + per-AC table + gallery link). Returns PASS/FAIL + evidence + findings in two lanes + `posted_comment`. **If `posted_comment:false`**, the conductor posts that comment in QA's stead. **Re-QA runs in delta mode** (failed ACs + targeted sweep only), re-spawned with the **FIX-CYCLE PACKET** (§"Fix-cycle packet" below).
 
 **[8b] Triage QA findings — two lanes, three outcomes.** QA returns exactly two lanes (`task_bugs[]`, `regression_bugs[]`); a finding ends as a task-bug sub-task, a PO-filed regression Bug, or a fix-in-run item.
 
@@ -210,8 +210,6 @@ FIX-CYCLE PACKET (conductor → any re-spawned agent):
 - findings_to_fix: the exact findings[] / failed ACs routed to you (ids + file:line)
 - decisions_already_made: solution-doc pointer + any [2b]/NEEDS_DECISION answers
 - open_items: unresolved followups[] relevant to you
-- advisor_answers: [{qid, reply_path, answer_one_line}] — the advisor consults answered
-  for you since your last envelope (§"Advisors" below); empty when none
 - DO NOT re-explore beyond these files unless a finding requires it — trust the
   packet, verify only what you touch.
 ```
@@ -222,9 +220,9 @@ This is the floor under every re-spawn regardless of lane, and the **mandatory f
 **Task-bug re-review trigger (NEARS-2043).** When this packet routes an [8b] task-bug fix to the implementing engineer — the one fix-cycle type that does not already re-enter [7] by definition (the [7] findings loop always does; [8] delta re-QA never touches review) — the re-spawned engineer's envelope MUST carry `change_class: code|comment-only|doc-only` alongside the fields above. See §[8b]'s re-review trigger for the default, the exemption spot-check, and the [9] DoD box it feeds. This adds one field to the returned envelope; the packet's own `findings_to_fix`/`your_previous_work` fields are unchanged.
 
 ## Advisors — standing read-only domain sessions (pilot: NEARS-3122 / NEARS-3124 / NEARS-3125)
-Design of record: `tasks/advisors/00-advisors-design.md` (§7 protocol · §9 trail · §10 tiers · §12 pilot). Roster: `docs/workflow/advisors/roster.yaml` (phase 1: `advisor-backend`, `advisor-flutter`, `advisor-dls`, `advisor-product`; the roster's domain keywords pick the advisor). **You — the conductor — are the ONLY asker:** no phase agent (nears-qa included) holds `SendMessage`/`ListAgents`, so every consult is conductor-driven; an advisor holds no Write and no Bash (a three-part lock at launch — design §5), so it only answers and you write the trail. **Pilot rule (design §12): every consult point is OPTIONAL.** Skip one → `trail[]` gets `advisor_skipped{phase, reason}`. Advisor absent from `ListAgents` → `bash scripts/advisors/up.sh <name>` ONCE, else skip. Standing advisor sessions load the brief at launch; after a budget/brief change run `down.sh`/`up.sh` so live sessions match the merged text. Deadline passes → proceed without the answer and record `advisor_timeout{qid, advisor}` in the run file's `drift[]` and your own envelope. **Never wait past the budget.** **Exception — tier gates ([2b] here, [3] in `/run-sprint`) are fail-closed:** a timeout or absence there never proceeds; each gate states its own retry-then-owner fallback.
+Design of record: `tasks/advisors/00-advisors-design.md` (§7 protocol · §9 trail · §10 tiers · §12 pilot). Roster: `docs/workflow/advisors/roster.yaml` (phase 1: `advisor-backend`, `advisor-flutter`, `advisor-dls`, `advisor-product`; a `NEEDS_DECISION` names its advisor by `domain`, the roster's domain keywords pick one for your own ad hoc question). **You — the conductor — are the ONLY asker:** no phase agent (nears-qa included) holds `SendMessage`/`ListAgents`, so every consult is conductor-driven; an advisor holds no Write and no Bash (a three-part lock at launch — design §5), so it only answers and you write the trail. **Pilot rule (design §12): outside the [2b] gate an ask is OPTIONAL** — answer a Tier A item yourself → `trail[]` gets `advisor_skipped{phase, reason}`. Advisor absent from `ListAgents` → `bash scripts/advisors/up.sh <name>` ONCE, else skip. Standing advisor sessions load the brief at launch; after a budget/brief change run `down.sh`/`up.sh` so live sessions match the merged text. Deadline passes → proceed without the answer and record `advisor_timeout{qid, advisor}` in the run file's `drift[]` and your own envelope. **Never wait past the budget.** **Exception — tier gates ([2b] here, [3] in `/run-sprint`) are fail-closed:** a timeout or absence there never proceeds; each gate states its own retry-then-owner fallback.
 
-**Consult points.** [2] the TL's `questions[]`, answered BEFORE it writes the solution doc · [6] the engineer packet (`questions[]` from the build envelope → `advisor_answers[]` in the next packet) · [8] the compiled QA Test Scope — asked by YOU to the domain advisor BEFORE spawning QA and folded into the scope QA receives; QA's own `questions[]` on return route through you into the delta re-QA packet · ad hoc, whenever you hold a domain question yourself. Any agent may return `questions[]` in its envelope — `{question, domain: backend|flutter|dls|product, files_in_play[]}` — and never waits for the answer itself.
+**Consult points — exactly three.** (1) A `NEEDS_DECISION{question, options, recommendation, domain}` from ANY phase agent: you classify the tier (design §10) and route a Tier A/B item to the roster advisor named by `domain` (§7 packet below); Tier C parks. (2) The [2b] tier gate (fail-closed, `advisor-product`). (3) Ad hoc — when YOU hold a domain question you would otherwise put to the owner. Nothing else asks: no pre-doc TL consult, no engineer-packet round-trip, no pre-QA scope review. Advisors answer questions the workflow would otherwise put to the owner; they are not a review step.
 
 **Packet (design §7, verbatim)** — one question per message, `SendMessage(to:"advisor-<name>", notify_when_idle:true)`:
 ```
@@ -232,7 +230,7 @@ ADVISOR QUESTION (conductor -> advisor-<name>):
 - qid: q-20260903-143012-nears-5e-NEARS-3041-001
 - ticket: NEARS-3041
 - phase: 6
-- question: <one question, verbatim from the subagent's questions[] entry or the conductor's own>
+- question: <one question, verbatim from the NEEDS_DECISION that raised it, or the conductor's own>
 - files_in_play: [paths the phase is touching]
 - deadline: <sent time plus 900 s, plus 1200 s when phase is 8>
 ```
@@ -255,7 +253,7 @@ elapsed_s: <int>
 phase: <phase>
 ticket: <NEARS-key>
 ```
-`sent_at` is DERIVED from the qid's embedded timestamp (`q-YYYYMMDD-HHMMSS-…` is authoritative; qid timestamps are UTC), `received_at` is a clock read at write time, and the file's mtime is the fallback when `received_at` is absent; then the remaining header items (asker, sha, model, spot-check result, timeout flag) · the question verbatim · the answer verbatim — never committed, outside every worktree. Then append the `consults[]` entry (§"Run state") + one `trail[]` line, and hand the answer on as `advisor_answers[]: [{qid, reply_path, answer_one_line}]` in the next fix-cycle packet.
+`sent_at` is DERIVED from the qid's embedded timestamp (`q-YYYYMMDD-HHMMSS-…` is authoritative; qid timestamps are UTC), `received_at` is a clock read at write time, and the file's mtime is the fallback when `received_at` is absent; then the remaining header items (asker, sha, model, spot-check result, timeout flag) · the question verbatim · the answer verbatim — never committed, outside every worktree. Then append the `consults[]` entry (§"Run state") + one `trail[]` line, and hand the answer on in the next packet's `decisions_already_made`.
 
 **Decision tiers — the ONLY tier table in the workflow. The item list lives in design §10 and is cited, never restated.**
 
