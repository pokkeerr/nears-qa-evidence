NEARS-1861 automated backstop — full UserApp suite at commit 11500328
Invocation: /Users/Apple/Tools/flutter/bin/flutter test --reporter json
cwd: <worktree>/UserApp   (NO path args; packages/nears_dls/test/ NOT included -- confirmed:
all 443 suite paths resolve under <worktree>/UserApp/test/, zero under packages/)
Counting rule NEARS-2167 (hidden-FLAG): 4631 testDone events, 452 hidden
'loading '/'compiling ' pseudo-tests excluded.

RESULT: 4179 real tests -- 4177 passed / 2 skipped / 0 FAILED / 443 suites

Skips BY NAME (both pre-existing, unchanged):
  - mixed-claim row alignment a no-claim tile lines up with its pilled neighbour @1.0
  - mixed-claim row alignment a no-claim tile lines up with its pilled neighbour @1.3

This ticket's 3 test files contribute 29 tests:
  module_null_cache_id_test.dart        15
  cache_id_byte_identity_test.dart      10  (5 pre-existing + 5 new null-key)
  favourite_module_id_filter_test.dart   4

RECONCILIATION GAP (reported, not silently absorbed): the spawn brief quotes a
baseline of 3914 real tests / 443 suites. Scope and suite count match exactly
(443, UserApp/test only), but the test count differs by 265 and cannot be explained
by this ticket's 24 net-new tests. git ls-tree shows 440 *_test.dart files at base
82bcaae1 vs 443 at tip, so a 443-suite baseline was itself measured at/near the tip,
not at base -- i.e. the brief's (3914 tests, 443 suites) pair is internally
inconsistent. The QA-relevant facts are scope-stated and unambiguous: 0 failures,
2 skips matching the documented names.
