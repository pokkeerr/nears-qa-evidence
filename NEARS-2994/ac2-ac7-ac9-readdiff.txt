=== AC2: all 4 Http:: call sites carry ->timeout(10) ===
Admin/app/Traits/NotificationTrait.php:190:                $response = Http::timeout(10)->withHeaders($headers)->post($url, $data);
Admin/app/Traits/NotificationTrait.php:232:            $response = Http::timeout(10)->asForm()->post('https://oauth2.googleapis.com/token', [
Admin/app/CentralLogics/Helpers.php:1604:            $response = Http::timeout(10)->withHeaders($headers)->post($url, $data);
Admin/app/CentralLogics/Helpers.php:1659:            $response = Http::timeout(10)->asForm()->post('https://oauth2.googleapis.com/token', [

=== AC7: git diff Admin/.env vs base — must be zero hunks ===
(zero output above == zero hunks, confirmed)

=== AC9: job constructor — scalars only, never $request ===
    public function __construct(
        private readonly int $orderId,
        private readonly ?string $email,
        private readonly ?string $name,
    ) {
