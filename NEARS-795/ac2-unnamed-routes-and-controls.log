# AC2 — unnamed routes (dialogs / bottom sheets) do not emit and do not throw

## UserApp: item-detail bottom sheet + notification-confirm bottom sheet + Login/Sign-Up sheet
Sheets opened and dismissed; navigation continued normally afterwards.

## Framework-exception count across the whole UserApp session (run4)
0

## Framework-exception count across the DeliveryApp session (run2)
0

## Detector positive control — the same grep DOES match a synthetic pre-fix line
input : ga_screen(_sn)=/reset-password?phone=+9715XXXXXXXX&token=NNNNNN
grep -c "ga_screen(_sn)=[^,}]*?" -> 1
same grep over the 35 real events -> 0
