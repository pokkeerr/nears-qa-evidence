NEARS-3646 QA — pre-existing, unrelated to this ticket's diff — logged as a regression note only.

On the Password step (Arabic UI), the DLS `NPasswordStrengthBar`'s accessibility label hardcodes
the English string "Password strength" verbatim:

  packages/nears_dls/lib/elements/npasswordstrengthbar/n_password_strength_bar.dart:131
    'Password strength${labelText.isEmpty ? '' : ': $labelText'}',

Confirmed this file carries ZERO diff on this ticket's branch (`git log` shows only
c6db8571c "feat(NEARS-1371): migrate NearsPasswordStrengthBar -> NPasswordStrengthBar (nears_dls)"
touching it; `git diff` against this ticket's base is empty for the file). NEARS-3646 correctly
REUSES this shared DLS component (AC-DLS/AC-SHARED intent) rather than duplicating it — the
untranslated label is the shared component's own pre-existing defect, owned by NEARS-1371's
lineage, not introduced here.

Impact: an Arabic screen-reader user hears "Password strength" in English on every screen that
uses this DLS element (this ticket's Password step + NEARS-3647's vendor journey + anywhere else
already reusing it).
