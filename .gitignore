# Scrub policy — keep internal page-source dumps and raw env captures OUT of this public repo.
# Admin-dashboard HTML var-dumps (Symfony Sfdump) — internal page sources, not visual evidence.
*.html
# Raw env / secret files, if any ever land here by accident.
.env
*.env
*secret*
*credential*
*token*.txt
