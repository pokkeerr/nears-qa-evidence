# NEARS-3041 QA progress (fix_cycle 0) — sha f73a0846a, device emulator-5556, backend :8000

- AC1 layering guard: PASS — exit 0, `check_layering: 6 allow-listed warning(s), 0 new violation(s).` + `PASS: vertical-layering invariant holds.`
- AC2 flutter analyze (pinned ~/Tools/flutter 3.41.9): PASS — 0 warnings, 0 errors, 3 pre-existing info `use_null_aware_elements` in cart_repository.dart:154/200/252 (file not in diff → on base).
- AC4 key diff: PASS — `AppConstants.seenModuleIds` → splash_repository.dart (4 uses: 295/300/309/317) + app_constants.dart:384; literal `'seenModuleIds'` → 1 hit; controller 0 hits for shared_preferences / SharedPreferences. Base had the 4 uses in the controller → same count moved, no new key.
- AC3 full suite: PASS — `03:00 +5620: All tests passed!`, rc 0, 690 test files (`find test -name '*_test.dart' | wc -l`); explicit run of `splash_repository_seen_module_ids_test.dart` + `module_controller_test.dart` = 102 tests, all pass; the new file's 8 tests named in expanded reporter (a: source pin; b: 5 seed-once; c: 2 failure-path sentinel).
- AC5 device: fresh install (suffixed pkg `com.izzes.nears.nears_nears_3041_module_controller_prefs`, key absent at first launch) → key seeded at splash pre-zone with every id of the FIRST `/api/v1/module` response `["1","2","3","5"]` (== no-zone API response). Home module row (zone 1) lists 1,2,3,9. Shot `ac5-home-module-row-fresh-install.png` + dump `ac5-home-a11y-dump.xml`: NO dot on Grocery/Food/Pharmacy; a mint New dot IS on `NEARS-2897 Shop (Ecommerce)` (id 9, created 2026-09-03 06:05 by sibling ticket, zone-1-only → absent from the pre-zone baseline). Seed-once guard unchanged from base → pre-existing NEARS-1508 semantics, not this change (regression_bug).
- AC6 device: `am force-stop` → key byte-identical (cmp xml-1 vs xml-2); relaunch → 2 module fetches HTTP 200, key still byte-identical (xml-3), no [FAIL]/[ERR] from pid 10243; tap Shop tile → key = `["1","2","3","5","9"]` (markModuleSeen → saveSeenModuleIds → repo); tap Pharmacy → unchanged; no crash, no [FAIL]. Shot `ac6-module-row-after-tap-shop-then-pharmacy.png`.
- AC8 static: no `print`/`debugPrint` in any `+` line of the lib diff; 1 `AppLogger.failure(const ModuleSeenPrefsFailure(), st, 'module-seen baseline seed failed')` in repo; unit-pinned by group (c) of `splash_repository_seen_module_ids_test.dart` (failed-future + sync throw → `[FAIL] … type=ModuleSeenPrefsFailure`, no raw exception text).
- AC7 device: address → saved Abu Dhabi (zone 2) → module row refreshed to Grocery/Food/Pharmacy (Shop gone; zone-2 API = [1,2,3,5]); 2 zone-id + 1 module fetch HTTP 200; key byte-identical (no re-seed). Sweep: zone-3 pick from a per-module home → existing "service not available" dialog (`[WARN]`, not FAIL); from the cold-relaunch landing → `sectors_shown {sectors_count: 1, zone_id: 3}` single-sector auto-select → module row hidden (hide-at-one). Whole run `[FAIL]`/`[ERR]` across pids 9029/10243/11351: exactly 1 — `LocationPermissionResolutionTimeout` fallback breadcrumb while I held the permission dialog >6s (documented expected breadcrumb, unrelated to ModuleController). Zero from ModuleController/ModuleSeenPrefsFailure.
- Verdict: PASS. Pre-existing finding → regression_bugs (bug-new-dot-on-zone-only-module-after-pre-zone-baseline.log).
