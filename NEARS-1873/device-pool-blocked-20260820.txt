NEARS-1873 — phase [8] live QA — device pool exhausted, verdict BLOCKED

Run started ~2026-08-20T22:0x (local). Worktree:
/Users/Apple/Projects/nears-NEARS-1873-uifind-fullscreen-barrier
commit under test: 062d8478 (base 8a512078 on feat/userapp-reskin2)

Automated backstop RAN and PASSED (independent re-run, not trusted from the
conductor's claim alone):
  python3 scripts/app-nav/test_uifind.py   -> Ran 45 tests in 0.002s, OK
  bash scripts/app-nav/uinav.test.sh       -> result: PASS; validity: 146
    assertion(s) across 21 case(s); 0 skipped; 0 failed (includes new cases
    17e/17f/17g for the rc=5 barrier branches)

Live device demonstration of AC1 (Wallet filter-popup recipe) and the
additional QA points (ZoneWarningDialog barrier, ui_home 2-3 screen sweep,
ui_find exit-3 pass-through check) could NOT be performed: all 3 pool
devices were occupied/locked by live, stable foreign sessions at dispatch
and stayed that way through a bounded poll.

adb devices at start of QA:
  emulator-5554   device   (attached)
  emulator-5556   device   (attached)
  emulator-5558   NOT attached (cold / not booted)

Device-by-device:

  emulator-5554 — no owner.json in $HOME/.nears/qa-locks/emulator-5554/, BUT
  scripts/qa-lock-guard.sh's own occupancy probe (NEARS-1805: absence of a
  lock file is not absence of a user) refused the acquire:
    verdict=occupied; devices_enumerated=2 signals_answered=3
    indeterminate=0 drivers=2(foreign) self_driven=0 excluded=0 mentions=2
    app_procs=1
    host drivers (not this session):
      pid 64231  flutter run -d emulator-5554 --debug   (started 2026-08-20
                 01:12:09, live at time of check — this is very likely the
                 process the NEARS-1873 engineer used for the build-phase
                 capture already sitting in this evidence dir,
                 wallet-filter-popup-dump.xml, timestamped
                 2026-08-20T17:41:51Z on the same serial)
      pid 64339  adb -s emulator-5554 shell logcat -v time  (child of 64231)
    device proc: com.izzes.nears:10060 (live app process on-device)
  -> NOT granted. Did not mkdir a lock, did not boot on it.

  emulator-5556 — locked. owner.json:
    {"pid": 5228, "pid_kind": "anchor", "anchor_started":
     "Tue Aug 18 20:22:34 2026", "key": "NEARS-2205", "lane": "NEARS-2205",
     "started": "2026-08-20T18:02:30Z"}
    pid 5228 confirmed LIVE (`claude --dangerously-skip-permissions`,
    started Tue Aug 18 20:22:34 2026) -> genuinely held, not stale/reclaimable.

  emulator-5558 — locked. owner.json:
    {"pid": 10132, "pid_kind": "anchor", "anchor_started":
     "Tue Aug 18 20:27:15 2026", "key": "NEARS-2130", "lane": "NEARS-2130",
     "started": "2026-08-19T14:34:18Z"}
    pid 10132 confirmed LIVE (`claude --dangerously-skip-permissions`,
    started Tue Aug 18 20:27:15 2026) -> genuinely held, not stale/reclaimable,
    even though the device itself is not currently attached to adb (owner's
    session may have the emulator paused/killed but is still an active
    session against this serial per the lock).

Bounded poll performed (NEARS pool protocol: "poll ~60s up to 10 min, then
BLOCKED"): 7 checks total across two loops, spaced 30-45s apart, spanning
~4.5 minutes wall-clock. All three occupancy signals (pid 64231, pid 5228,
pid 10132) stayed alive at every check; emulator-5558 never reappeared in
`adb devices`. Given all three occupants are stable, hours-to-days-old live
sessions (not mid-transition), further polling toward the full 10-minute
bound was judged low-value and stopped early per the cost-discipline
guidance ("never spin forever").

No lock was acquired by this QA run on any device, so none was released
(nothing to release). A fourth serial, emulator-5560, is documented pool-
excluded (NEARS-1727, broken uiautomator bridge) and was not considered.
Two additional local AVDs exist (nears_qa_delivery, nears_qa_wave56) but are
outside the documented 3-device pool in .claude/workflow-profile.md; booting
one was judged outside this agent's authority to improvise and is flagged in
drift[] for the conductor instead.

Conclusion: verdict BLOCKED{queue: emulator-5554 occupied by a live foreign
flutter-run (no lock file, NEARS-1805 residue detection), emulator-5556
held by NEARS-2205 (pid 5228 live), emulator-5558 held by NEARS-2130 (pid
10132 live, device currently cold)}. AC1 and the additional live-device QA
points are UNDEMONSTRATED, not failed — this blocks Done per the QA gate's
own hard rule ("couldn't run it" is FAIL/BLOCKED with the reason, never a
PASS on green tests alone). AC2 (automated suites) independently
re-verified PASS. AC3 is explicitly out of scope for this build per the
dispatch brief.
