# AC1 — grep 'json_decode' in BannerLogic.php (expect NO matches):
(no matches — AC1 PASS)

# AC2 — grep 'parseZoneIds' in BannerLogic.php (expect L30 + L70):
30:                            ->whereIn('zone_id', Helpers::parseZoneIds($zone_id)); // NEARS-693: fail-closed zone decode (mirrors NEARS-549)
70:                            $query->whereIn('zones.id', Helpers::parseZoneIds($zone_id)); // NEARS-693: fail-closed zone decode (mirrors NEARS-549)
